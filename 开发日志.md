# 开发日志

本文件记录本项目的**每一次开发操作**：改了什么、为什么改、怎么验证的。
目的是让任何人在任何时间点都能读懂项目的演进过程，而不必去逐条翻 `git diff`。

> 项目概览与使用方式见 [`README.md`](./README.md)。

---

## 使用约定

**写入时机：每次修改项目文件，必须在同一次操作里往本文件追加一条记录。**
不写日志的改动，视为没做完。

**条目模板**（复制这段填写）：

```markdown
### 简短标题（动词开头，如「补齐前端实现」）
- **类型**：功能 / 修复 / 重构 / 视觉 / 性能 / 文档
- **文件**：`web/index.html`（多个用「、」分隔）
- **改动**：
  - 一条一句，说清「改了什么」
- **原因**：为什么要改（问题现象 / 需求来源）
- **验证**：怎么确认改对了（命令、观测指标、实测数值）
- **坑与教训**：踩到的陷阱、试过但放弃的方案及原因（没有就删掉这一行）
```

**规则**

- **最新条目在最上面**：`## 日期` 倒序，同一天内的条目也倒序，方便一眼看到最近发生了什么。
- 日期格式 `YYYY-MM-DD`，与 `git log` 对齐。
- 「验证」必须写**可复核的证据**（命令输出、测得的数值），不要只写「已测试通过」。
- 「坑与教训」是最有价值的部分 —— 重点写**走不通的路和原因**，避免后人重复踩。
- 一次大改动拆成多条（一条一个主题），不要把不相关的事塞进同一条。
- 本文件**人工维护**，不要自动生成、也不要是脚本的输出目标。

---

<!-- 新条目插入到这一行下方 ↓（最新的在最上面） -->

## 2026-09-14

### 提交 v1.0 到 git
- **类型**：文档 / 发布
- **文件**：`.gitignore`、全部既有改动
- **改动**：
  - `.gitignore` 新增 `data/snapshots/` —— 抓取快照是可重新生成的存档
    （每次 CLI 运行都会新增一个目录），不入库。
  - 提交 `172efc0`：15 个文件、**+24362 / −159**。
    含新增的 `server.mjs`、`web/index.html`、`开发日志.md`、`data/fixtures/`（5 个）、
    `data/trn-sessions.json`（584K），以及 `track.mjs`（+1315 行）等既有文件的改动。
- **原因**：用户确认「第一阶段开发完毕，版本 1.0」并批准按建议提交。
- **验证**：
  - **提交前安全扫描**：
    - 确认暂存清单**无 `.env`、无 snapshots**；
    - `grep -rilE 'api[-_]?key|token|secret|password'` 命中的 2 处经逐一核对，
      都是**注释里提到的字面量 `TRN_API_KEY`**（如「没有 TRN_API_KEY 的情况下验证…」），
      不是真实密钥值；
    - `grep -oE '[A-Za-z0-9_-]{32,}'` 检出的长串是**比赛 UUID**（`4a660b6d-de1f-…`），
      属公开数据，非密钥。
  - **提交后**：`git log` 顶部为 `172efc0`，`git show --stat` 显示 15 files changed。
  - **未推送**：`origin/main` 仍指向 `5829cae`（等用户确认后再推）。
- **说明（一处已知的不一致）**：`data/snapshots/` 下**3 个旧快照此前已被跟踪**，
  本次只忽略新增的 4 个。所以现状是「旧的在库里、新的不进库」。
  若要彻底一致，需要 `git rm --cached` 把那 3 个也移出跟踪（文件仍留在磁盘）——
  **未执行**，待用户决定。
- **坑与教训**：
  - **`.gitignore` 只能阻止"未来入库"，不会影响已跟踪的文件**。
    所以「加一条忽略规则」并不能让仓库变得干净的 —— 要先确认该路径下有没有已跟踪的内容。
  - **提交前做机械化的敏感信息扫描**比凭记忆判断可靠。
    这次的两个命中都是误报，但**逐一核对过才敢下结论**；
    如果只看到「有命中」就跳过，或者看到「是误报」就一律忽略，都可能漏掉真实泄漏。
  - 隐私上无新增暴露：仓库里**早已提交过** `data/history.jsonl` 与 `Apex 战绩.md`
    （含玩家名 / RP / 等级），且 Apex 战绩本身是公开可查的，所以 `trn-sessions.json`
    与既有惯例一致。

### 🏁 v1.0 发布（第一阶段完成）
- **类型**：里程碑
- **文件**：`track.mjs`、`server.mjs`、`web/index.html`、`README.md`
- **改动**：
  - 版本标识统一为 **v1.0**：
    - `track.mjs` 文件头重写（原写「v1.1 / als 为默认 / trn 需 Key」**已全部过期**）+
      `--help` 文本同步（原写「ALS 无需 Key」「trn 需 TRN_API_KEY」也是错的）。
    - `server.mjs` 文件头 `v0.1 → v1.0`，接口清单补上 `matchesSource` 与 `access-control` 说明、
      新增 `APEX_TRACK_TIMEOUT_MS` 环境变量说明。
    - `web/index.html` footer 加 `Apex 战绩查询 v1.0` 徽标；同时**纠正过期的数据来源说明**
      （原写「数据来源：Apex Legends Status」，实际已改为 tracker.gg）。
    - `README.md` 加版本行与「功能一览」表。
- **原因**：用户宣布「第一阶段开发完毕，版本 1.0」。
- **验证（发布前全量检查）**：
  - **离线测试**：`verify-als-playlist` **23/23**、`verify-playlist` **11/11**、
    `verify-trn-page` **9/9** —— 合计 **43/43**。
  - **语法**：`node --check track.mjs` / `server.mjs`、前端 `<script>` 均通过。
  - **冒烟（真实数据）**：`ok: true`、`provider: trn`、玩家 `L-icet`、
    段位 `Diamond 4`、RP `12,368`、当前赛季 `S30 诸神烙印`（与 tracker.gg 一致）、
    87 个会话 / 5 个赛季。
  - **服务**：`GET / → 200`，`server` 进程 1 个、`track.mjs` 残留 0。
  - **`--help` 输出**：与实现一致（默认 provider 已改为 trn、ALS 标注「对局滞后」）。
- **v1.0 功能清单**：
  | 区块 | 内容 |
  |---|---|
  | Current Rating | 段位徽章、RP、Top 百分位、排名、**本赛季/历史峰值 RP** |
  | Account | 等级 / 转生 / 传奇数 / 追踪器 |
  | 最近表现 | RP 走势图（手写 SVG）、**赛季分段线 + 图例**、赛季选择器（默认最新赛季） |
  | 常用传奇 | 按出场数统计的传奇使用排行（独立页签） |
  | 对局记录 | 会话列表、**相对时间 + 绝对日期**、排位/匹配徽标与筛选、**按赛季分组**、赛季筛选（默认最新赛季） |
  | 数据对比 | tracker.gg 采集指引（一键复制脚本）+ 与后端的一致性校验（独立页签） |
- **坑与教训**：
  - **文件头的「版本说明」最容易腐烂**。这次发现 `track.mjs` 头部还写着
    「v1.1 / als 为默认」、`--help` 还写着「trn 需 TRN_API_KEY」、
    前端 footer 还写着「数据来源：ALS」—— 三处都是**功能已改而文档没跟上**。
    → **发布前专门过一遍所有"面向人的说明文字"**（文件头注释、`--help`、
      界面文案、README），把它们当代码一样对待。
  - **「什么算完成」需要可执行的判据**。这次用「43 条断言 + 语法检查 + 真实数据冒烟 +
    进程健康」四项作为发布门槛，而不是凭感觉说「差不多了」—— 这样回归时也有据可依。


- **类型**：视觉 / 修复
- **文件**：`web/index.html`
- **改动**：
  - **背景**：上一轮已把相对时间移到行的第一行，但用户**第二次**反馈「没看到显示」。
    核查后确认**功能一直在渲染**（`curl` 服务端 HTML 含 `class="when"` 与 `function relTime`，
    `Cache-Control: no-store` 不缓存；浏览器实测每行第一行都有 `2 天前`）。
    → 判定为**可见度仍然不足**，于是主动加强。
  - 新增 `absShort(ts)`（`09/12 04:15`）与 `whenLabel(ts, extra)`：
    **相对时间用亮色加粗（`--ink`, 700）+ 绝对日期用暗灰（`--ink-3`）并排**：
    ```
    2 天前  09/12 04:15        ← <b>2 天前</b><span>09/12 04:15</span>
    动力小子 [会话] [未定]
    ```
    - 两者并列是刻意的：tracker.gg 只给相对时间，但用户问过「当天还是几天前」，
      单看相对时间不够有把握，配一个绝对日期做参照最直观。
    - 字号 11.5px（原 11px）、`<b>` 用 `--ink`（`rgb(234,232,227)`，接近白色）加粗，
      `<span>` 用 `--ink-3`；`title` 保留完整时间 `2026/9/12 04:15:02`。
  - `trnRow` 与 `matchRow`（ALS）都改用 `whenLabel()`，去掉各自的临时拼装；
    ALS 侧若无时间戳则退回它自带的相对文案（`m.when`）。
- **原因**：用户第二次反馈「对局记录里我咋没看到显示，你没有添加嘛？」。
- **验证**：
  - **证明功能存在**：`curl http://127.0.0.1:8787/` 返回的 HTML 中
    `class="when"` 出现 **2 次**（TRN/ALS 两个渲染函数各一）、`function relTime` 存在、
    响应头 `Cache-Control: no-store`。
  - **结构**：`<div class="when" title="2026/9/12 04:15:02"><b>2 天前</b><span>09/12 04:15</span></div>`
  - **样式**：`<b>` = `rgb(234,232,227)` / 11.5px / 700；`<span>` = `rgb(107,115,124)` / 10.5px。
  - **页面实测**：14 行全部有时间，值为
    `2 天前 / 5 天前 / 5 天前 / 1 周前 ×3 / 2 周前 ×7 / 1 个月前`。
  - **移动端 390px**：时间完整显示、无截断（3 行 `scrollWidth > clientWidth` 全为 false）、
    行高 70~71px、在卡片内。
  - **回归**：三份离线测试 **43/43**；前后端语法通过；横向溢出 0。
- **坑与教训**：
  - **用户第二次说「没看到」时，不要只回复「已经实现了」**。
    要先给出**可验证的证据**（服务端响应内容 + 浏览器里的实际 DOM），
    再主动判断是「真的没有」还是「有但不够显眼」—— 这次是后者，
    所以做的是**加强可见度**而不是重写逻辑。
  - **「信息存在」≠「信息被看见」**。11px 灰字放在灰色 meta 行里被忽略过两次，
    说明视觉权重不够。**用「对比度 + 字重 + 并列参照」三件套提升**：
    相对时间加粗提亮、绝对日期做参照、整行占位。
  - 相对时间单独出现时读者会有不确定性（「3 天前」是几天前？），
    **配一个绝对日期做锚点**能消除这种模糊 —— 尤其在这个以「读数」为设计语言的界面里。
  - **不要依赖「刷新一下就好」这个解释**。即使真是缓存，也应该同时让实现本身更健壮，
    否则下次还会遇到同样的困惑。

  > 上一轮「移到第一行」的改动记录如下（当时用户反馈的第一次）。

### 对局时间加大可见度：相对时间（亮）+ 绝对日期（暗）并列
- **类型**：视觉 / 修复
- **文件**：`web/index.html`
- **改动**：
  - 用户**第二次**反馈看不到时间 → 核实功能一直在渲染（`curl` 服务端 HTML 含
    `class="when"` 与 `function relTime`，`Cache-Control: no-store`；浏览器每行都有 `2 天前`），
    判定为**可见度不足**，于是加强。
  - 新增 `absShort()` 与 `whenLabel()`：**相对时间亮色加粗（`--ink` / 700）+
    绝对日期暗灰（`--ink-3`）并列**；字号 11px → 11.5px。
  - `trnRow` 与 `matchRow` 共用 `whenLabel()`。
- **验证**：14 行全部有时间；样式实测 `<b>` = `rgb(234,232,227)`、`<span>` = `rgb(107,115,124)`；
  移动端 390px 无截断；回归 43/43。

### 对局时间改为「行的第一行标签」+ 支持「今天/昨天」
- **类型**：修复 / 视觉
- **文件**：`web/index.html`
- **改动**：
  - **问题定位**：用户反馈「对局记录里没有显示对局时间」。
    实测相对时间**确实已经渲染**（`2 天前 · 1 场 · 会话合计`），
    但它埋在**灰色小字（10.5px / `--ink-3`）的 meta 行里**，被名字和徽标压住，实际看不到。
    → 不是没实现，是**可见性不足**。
  - **改为 tracker.gg 的行结构**（时间在最上方作为行标签）：
    ```
    改前：  动力小子 [会话] [未定]        ← 名字在上
            2 天前 · 1 场 · 会话合计      ← 时间埋在灰字里

    改后：  2 天前                        ← .when（11px 等宽，--ink-2）
            动力小子 [会话] [未定]         ← .nm
    ```
    - 新增 `.mrow .when` 样式：11px 等宽、`--ink-2`（比原来的 `--ink-3` 亮一档）、
      与 `--font-data` 一致（呼应 HUD 的「读数」语言）。
    - TRN 行的「1 场」只在 `matchCount > 1` 时显示（绝大多数会话就是 1 场，恒显是噪音）；
      「会话合计」四字去掉（`会话` 徽标已表达）。
    - ALS 的逐场行同步改为三行结构：`时间` / `名字+徽标` / `模式 · 地图 · 时长`。
    - 绝对时间仍放 `title`（实测 `2026/9/12 04:15:02`）。
  - **`relTime()` 改为按日历天计算**并新增「昨天」：
    | 差值 | 显示 |
    |---|---|
    | < 60s / 未来时间 | `刚刚` |
    | < 60min | `N 分钟前` |
    | 同一天 | `N 小时前` |
    | 昨天 | **`昨天`** |
    | 2–6 天 | `N 天前` |
    | 7–29 天 | `N 周前` |
    | 30–364 天 | `N 个月前` |
    | ≥365 天 | `N 年前` |

    为什么改用日历天：按 24 小时差值会出现「23 小时前」其实是昨天、
    「25 小时前」其实只隔了一晚的困惑 —— **「今天/昨天」才是用户的心理模型**。
- **原因**：用户要求「对局记录里没有显示对局时间，tkgg 有显示对局时当天还是几天前、几个月前」。
- **验证**：
  - **结构**：每行第一行是 `.when`、第二行是 `.nm`（实测
    `["when:2 天前", "nm:动力小子会话未定"]`），`.meta` 已无时间残留（4 行全为 `null`）。
  - **可见性**：`.when` 为 11px / `rgb(164,171,179)`（`--ink-2`）/ 等宽字体。
  - **边界（14 例全过）**：未来 1 小时 → `刚刚`；30 秒 → `刚刚`；30 分 → `30 分钟前`；
    今天 00:05 → `5 小时前`；**昨天 12:00 → `昨天`**；2/6 天 → `N 天前`；
    7 天 → `1 周前`；29 天 → `4 周前`；30 天 → `1 个月前`；364 天 → `12 个月前`；
    365 天 → `1 年前`；`0` / `null` → 空。
  - **页面实测**：`2 天前 / 5 天前 / 5 天前 / 1 周前`。
  - **移动端 390px**：`.when` 正常显示，行高 70~71px，都在卡片内。
  - **回归**：三份离线测试 **43/43**；前后端语法通过。
- **坑与教训**：
  - **「没显示」有三种可能：没实现、没数据、被埋了**。这次是第三种 ——
    功能一直在跑，只是样式把它压成了看不见。
    → 用户报「看不到 X」时，**先截图/量样式确认 X 是否已渲染**，
    再决定是补功能还是改可见性。这比直接重写实现省事得多。
  - **重要信息要放在视觉动线上**。相对时间被塞进「name 下面那行灰字」，
    而那一行还有 `1 场 · 会话合计` 这类弱信息在抢注意力。
    tracker.gg 把时间**单独占一行放在最上方**，是有道理的 ——
    行的第一眼位置应该给「什么时候」这种用于定位的信息。
  - **信息取舍**：去掉恒为真的「会话合计」、只在 >1 时显示「N 场」。
    堆在界面上的恒定值只会稀释真正有变化的信息。
  - **相对时间的日级分界要用日历天**。纯 24 小时差值在边界附近会违反直觉。

### 加相对时间显示 + 按当前日期判断赛季
- **类型**：功能
- **文件**：`track.mjs`、`web/index.html`
- **改动**：
  - **加相对时间**（对齐 tracker.gg 的 `2 days ago`）
    - ⚠️ **数据里没有这个字段** —— 实测 TRN 的 `metadata` 只有 `startDate`/`endDate`/`duration`/`isActive`，
      没有 `ago`/`relative` 之类的现成值；**tracker.gg 也是前端自己算的**。所以新增 `relTime(ts)`。
    - 档位：`刚刚`（<60s）/ `N 分钟前` / `N 小时前` / `N 天前`（<7）/
      `N 周前`（<30）/ `N 个月前`（<365）/ `N 年前`。
    - **未来时间戳返回「刚刚」**：防时钟偏差导致的「−1 天前」这种滑稽显示。
    - 用在三处：对局行 meta（`2 天前 · 1 场 · 会话合计`）、赛季组头
      （`2 天前 · 08-10 12:32 → 09-12 04:15 · 第 1 段`）、ALS 的逐场行。
      绝对时间放进 `title` 备查。
    - 它是**相对当前时刻动态计算**的：同一份缓存数据放久了会从「2 天前」变成「2 周前」——
      这正是我们想要的行为（数据确实旧了）。
  - **按当前日期判断赛季**（`currentSeasonInfo()`，`track.mjs`）
    - 用系统时间调 `seasonOf(now)`，输出到 `record.seasonInfo`：
      `{ at, season, name, start, split, trackerSays, consistent, stale }`。
    - **为什么必须按系统时间算**：它是个动态值，不能从对局数据反推 ——
      玩家可能一两周没打，数据里的最新赛季会落后于真实当前赛季。
    - **与 tracker.gg 交叉验证**：用它的 `metadata.currentSeason` 对比
      （实测两边都是 30 → `consistent: true`）。这是最有价值的信号 ——
      内置赛季表是硬编码的，会随新赛季发布而过期，靠这个能自动发现。
    - **`stale` 标记**：当前时间超出内置表覆盖范围（最后一项 +100 天）时置真。
      此时 `seasonOf` 会把最后一项当答案返回，实际可能已经有新赛季了 ——
      必须提示，而不是默默给一个可能错的答案。
  - **前端显示「当前赛季」**（`web/index.html`）
    - 新增 `.season-now` **独立一行**：`当前赛季 S30 · 诸神烙印 · 第 1 段`。
    - 赛季按钮上给当前赛季加**青色圆点**（`.now-dot`），一眼看出「现在是这个赛季」。
    - 三种警告（都能在界面看到原因与处置方式）：
      | 条件 | 提示 |
      |---|---|
      | 数据最新赛季 < 当前赛季 | `⚠️ 数据最新只到 S30` |
      | `consistent === false` | `⚠️ 与 tracker.gg 不一致（对方报 S29），请更新 SEASONS` |
      | `stale === true` | `⚠️ 赛季表可能已过期，请更新 SEASONS` |
- **原因**：用户要求「在 tkgg 里有 2 days ago 这个显示，在我的项目里没有，显示他，
  然后添加查询当前日期判断是那个赛季」。
- **验证**：
  - **相对时间**：行内实测 `2 天前 / 4 天前 / 5 天前 / 1 周前`；
    组头 `2 天前 · 08-10 12:32 → 09-12 04:15 · 第 1 段`；`title` 给出 `2026/9/12 04:15:02`。
  - **边界（14 例全过）**：未来 1 小时 → `刚刚`；30 秒 → `刚刚`；59 分 → `59 分钟前`；
    1 小时 / 23 小时 / 1 天 / 6 天 → 相应值；7 天 → `1 周前`；29 天 → `4 周前`；
    30 天 → `1 个月前`；364 天 → `12 个月前`；365 天 → `1 年前`；`0` / `null` → 空。
  - **当前赛季**：`season: 30`、`诸神烙印`、`start: 2026-08-05`、`split: 1`、
    `trackerSays: 30`、`consistent: true`、`stale: false`。
  - **警告分支**（用合成 `seasonInfo` 逐一验证）：
    正常 → 只有当前赛季；当前 S31 而数据到 S30 → `⚠️ 数据最新只到 S30`；
    `consistent:false, trackerSays:29` → `⚠️ 与 tracker.gg 不一致（对方报 S29）…`；
    `stale:true` → `⚠️ 赛季表可能已过期…`；还原后恢复干净。
  - **布局**：`.season-now` 宽 996、单行、完整显示不截断（此前塞在筛选条 flex 行里被挤压，
    只显示到 `当前赛季 S30 ·`）；移动端 390 也单行；横向溢出 0。
  - **回归**：三份离线测试 **23+11+9 = 43/43**；前后端语法通过。
- **坑与教训**：
  - **不要把信息塞进已经"很挤"的 flex 行**。「当前赛季」最初写成
    `.mh-filter .cs{margin-left:auto}` 放在赛季按钮那一行，结果被挤压到只显示
    `当前赛季 S30 ·` 后面全被截断。改成独立一行就正常了。
    → **同一行里的元素一旦需要"独占剩余空间"，就会和已有内容竞争**；宁可多占一行。
  - **相对时间的"未来值"必须处理**。时间戳理论上不会超过现在，但客户端时钟偏差、
    或服务端时间不准都会造成负数差值，直接算会显示「−1 天前」。夹到「刚刚」即可。
  - **硬编码的数据表要自带失效检测**。内置赛季表迟早过期（新赛季发布后），
    所以加了 `stale`（超出覆盖范围）+ `consistent`（与上游交叉验证）两道检查，
    让过期能被自动发现并给出「更新 SEASONS」的指引，而不是静默给出错误答案。
  - 同一份 `seasonInfo` 对 ALS 模式也适用：`p.season` 不存在时 `trackerSays: null`、
    `consistent: null`，仍能算出当前赛季，只是少了交叉验证 —— 降级合理。

### 「对局记录」赛季筛选改为默认最新赛季
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - 赛季按钮原本已存在但默认是「全部」，改为**默认最新赛季**（与「最近表现」走势图一致）。
  - 沿用与走势图**完全相同**的「默认跟随 + 手动锁定」模式：
    ```js
    let seasonFilter = null;
    let seasonFilterTouched = false;   // 用户是否手动点过
    if (!seasonFilterTouched && seasonFilter !== latestSeason) seasonFilter = latestSeason;
    ```
  - **类型按钮的计数改为跟随所选赛季**。原先从 `all`（全部记录）统计，
    选了 S30 后按钮仍显示 `全部 87 / 排位 52 / 匹配 35`，与实际显示的 14 行不符。
    改为从 `inSeason`（仅赛季过滤后）统计 → `全部 14 / 排位 8 / 匹配 6`。
  - 汇总行（排位/匹配/净 RP）同样跟随赛季；「赛季 N 个」只在看「全部」时显示。
  - 副标题改为组合式：`S30 · 14 个会话` / `S24 · 仅排位 · 14 个会话` /
    `全部 5 个赛季 · 87 个会话`。
  - 组头「覆盖 N 个赛季」只在真跨赛季时显示（单赛季下是废话）。
  - 筛选后为空的提示补上「换个**赛季**或类型试试」；该分支保留筛选条，
    否则用户无法切回来。赛季按钮的「全部」补上计数与空格：`全部 5`。
  - `doQuery()` 复位新增 `seasonFilterTouched = false`（与 `trendSeasonTouched` 对称）。
- **原因**：用户要求「对局记录也添加按钮，显示赛季数据，默认显示最新赛季」。
- **验证**：
  - **默认**：标题 `对局记录 S30 · 14 个会话`，S30 按钮高亮，14 行；
    类型按钮 `全部 14 / 排位 8 / 匹配 6`；汇总 `排位 8 / 未定 6 / 净 RP +567 / 疯玛吉 3 个会话`。
  - **赛季切换**：S25 → `S25 · 34 个会话`，类型计数变 `全部 34 / 排位 19 / 匹配 15`，
    汇总 `排位 19 / 未定 15 / 净 RP +9,036`；「全部 5」→ `全部 5 个赛季 · 87 个会话`、87 行。
  - **筛选叠加**：S30 + 「排位」→ `S30 · 仅排位 · 8 个会话`、8 行；再切 S24 → `S24 · 仅排位 · 14 个会话`。
  - **换玩家复位**：先手动选 S23 → 重新查询 → 自动回到 **S30**（跟随最新），
    类型筛选也复位为「全部」，走势图的赛季同步为 S30。
  - **✅ 实际收益**：默认视图下页面高度 **7822 → 2416px**（卡片 6773 → 1367px），
    不用再滚过 87 行。
  - **移动端 390px**：两条筛选条各 24px（单行）、都在卡片内；14 行正常。
  - **回归**：三份离线测试 **23+11+9 = 43/43**；前后端语法通过；桌面横向溢出 0。
- **坑与教训**：
  - **筛选器之间的计数要跟着彼此联动**。第一版只改了「显示哪些数据」，
    没改「按钮上的数字」，结果选中 S30 后按钮仍写 `全部 87`，与实际 14 行自相矛盾 ——
    这类不一致用户一眼就能看出来。**只要有一个筛选生效，其它按钮的计数都要重新算。**
  - **测试时序又骗了我一次**：`waitForSelector('#matchCard .mh-sum')` 在导航后
    **匹配到了导航前的旧 DOM**（选择器依然存在），于是我读到了过期状态，
    一度以为「对局记录显示 S26、走势图显示 S30，两者不一致」。
    用 `loadCount` 标记验证后确认 `goto` 确实重新执行了脚本、两边都是 S30 —— **实现没问题**。
    → 教训：**导航后要等「新页面」的判据**（如某个只在新加载后存在的标记），
    不能只等一个前后都存在的选择器。这条已和之前的视口陷阱并列记入记忆。
  - 同一个「默认跟随 + 手动锁定」模式在一个页面里用了两次，
    变量命名保持对称（`seasonFilter`/`seasonFilterTouched` 与 `trendSeason`/`trendSeasonTouched`），
    便于日后一起维护。

### 「最近表现」加赛季选择按钮（默认只看最新赛季）
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - 走势图顶部新增**赛季选择器**：`赛季 [全部] [S30] [S26] [S25] [S24] [S23] 默认最新赛季`。
  - **默认只显示最新赛季**。原因：跨 5 个赛季画一条曲线时，值域从 3.5k 拉到 12.7k，
    每个赛季的起伏被压成平线，看不出节奏；只看 S30 时值域是 8,878~12.5k，起伏清晰。
  - 两个状态变量分工（关键设计）：
    - `trendSeason` —— 当前选中（`null` = 全部）
    - `trendSeasonTouched` —— 用户是否手动点过；**没点过则自动跟随最新赛季**
    ```js
    if (!trendSeasonTouched && trendSeason !== latest) trendSeason = latest;
    ```
    这样既满足「默认最新」，又不会在重新查询后把用户的选择弄丢、或停在已不存在的赛季。
  - 分段线与图例改为**按需渲染**：`trendSvg(pts, { segments })`，
    只有「全部」且真跨赛季时才画（单赛季下它们是重复信息）。
    区段标签也加了 `useSegLabels` 条件（单赛季标题已有赛季名，图里不再标）。
  - 副标题随选择变化：`S30 · 诸神烙印 · 14 场` / `全部 5 个赛季 · 87 场`。
  - 底部指标（净 RP / 场均 / 最好 / 最差）跟着所选赛季重算。
  - `doQuery()` 里把 `seasonFilter` / `trendSeason` / `trendSeasonTouched` 一并复位 ——
    上个玩家选中的赛季在新玩家身上可能不存在。
- **原因**：用户要求「添加按钮，可以选择某个赛季显示，默认显示最新赛季一个赛季」。
- **验证**：
  - **默认**：标题 `S30 · 诸神烙印 · 14 场`，按钮高亮 `S30`，
    14 个点、无分段线、无图例、X 轴 `08/10 → 09/12`、底部 `净 RP +567`。
  - **切 S24**：`S24 · 侵略 · 27 场`，27 点、X 轴 `03/09 → 05/04`。
  - **切「全部」**：`全部 5 个赛季 · 87 场`，87 点、**4 条分段线**、
    标签 `S24/S25/S26/S30`、**图例出现**、X 轴 `11/26 → 09/12`、底部 `净 RP +17,475`。
  - **切回 S30**：与默认完全一致（分段线/图例消失）。
  - **换玩家复位**：先在 S23 → 重新查询 → 自动回到 **S30**（跟随最新），
    而不是停在旧选择或空白。
  - **布局**：选择器宽 686、卡片 730，完全在卡片内；390 / 1100 / 1500 三档都不溢出
    （390 下的「溢出」是桌面版优先的横向滚动，属设计行为）。
  - **回归**：三份离线测试 **23+11+9 = 43/43**；前后端语法通过。
- **坑与教训**：
  - **「默认值」和「用户选择」要分开存**。如果只用一个变量，
    要么默认值会把用户的选择覆盖掉，要么用户的选择会在数据刷新后失效。
    加个 `Touched` 标志是这类「智能默认」的标准解法 —— 可以概括为
    **「默认跟随，一旦手动则锁定，换上下文再解锁」**。
  - **可视化元素的必要性取决于数据范围**。分段线与图例在跨赛季时必要，
    单赛季时纯属噪音 —— 所以让它们跟着选择走，而不是无条件渲染。
  - **筛选状态属于「上下文」**，换玩家/重新查询时必须复位。
    这已经是第二次遇到同类问题（上次是 `seasonFilter`），说明**新增任何筛选状态都要
    同步在 `doQuery` 的复位块里登记**。

### 「最近表现」走势图按赛季分段
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - `trendPoints()` 的输出带上 `season` / `name`，供图上分段。
  - `trendSvg()` 新增**赛季分段**：
    - 赛季切换处（相邻两点 season 不同）画**竖虚线**，位置取两侧点的中点（落在两场之间更自然）。
    - 顶部按区段居中显示赛季标签；**区段宽度 < 44 viewBox 单位就不画**，避免标签叠在一起。
    - 悬停 tooltip 补上赛季：`11/26 · 7,229 RP · S23`。
  - 新增 **赛季图例**（跨赛季时才显示）：每个赛季显示 `S30 诸神烙印 · 14 场 · +567`，
    净 RP 按涨跌着色。标题也改为 `87 场 · 5 个赛季 · RP 走势`。
  - 新增 `trendSeasonStats(list)`。
  - **修正一处口径不一致**：图例起初用「曲线首尾差值」算净 RP，
    与「对局记录」的赛季分组（`rpChange` 累加）**对不上**，实测能差好几千：

    | 赛季 | 曲线首尾差值（错） | rpChange 累加（对） |
    |---|---|---|
    | S23 | +2,496 | +5,525 |
    | S24 | +6,043 | +2,486 |
    | S25 | +2,607 | +9,036 |
    | S26 | +8,843 | **−139** |
    | S30 | +3,490 | +567 |

    → 改为**统一用 `rpChange` 累加**，并传入**全量 list**（不限于有 RP 的点）。
    首尾差值会被脏数据与时间断档影响，`rpChange` 才反映真实增减。
  - 分界标签字号按「最终落地像素」定：SVG 等比缩放，桌面卡片固定 730px（系数 ≈0.90），
    基字号 11px 落地仅 9.9px（比轴标签还小）→ 提到 **14px（落地 12.6px）**，
    容器查询档位同步改为 ≤640→18px、≤420→24px。
  - 清理死代码：移除未使用的 `secs` 与 `durText`（改用 tracker.gg 后没有时长数据了）。
- **原因**：用户要求「在最近表现页区分赛季」。
- **验证**：
  - **分段绘制**：4 条分界线（x = `96 / 311 / 583 / 638`）+ 4 个标签，
    **每个标签 x 都精确落在自己区段的中点**（S24=204、S25=447、S26=610、S30=694，
    对应各区段 `(96+311)/2`、`(311+583)/2`、`(583+638)/2`、`(638+746)/2`）；
    最小标签间距 84 viewBox 单位，**无重叠**（标签宽 30）。
  - **S23 未标**是刻意的：其区段仅 5 个点、宽度不足 44，标签会叠在一起。
  - **图例与分组一致**（逐项核对）：
    走势图 `S23 +5,525 / S24 +2,486 / S25 +9,036 / S26 −139 / S30 +567`
    ⟷ 对局记录分组 `+5,525 / +2,486 / +9,036 / −139 / +567` —— **5/5 全等**。
  - **可视性**：分界标签落地 12.6px（轴标签 11.7px），图例 2 行、5 项完整，均在卡片内。
  - 横向溢出 0（1500 / 1100 / 390 三档；390 下因桌面版优先而横向滚动，属设计行为）。
  - **回归**：三份离线测试 **23+11+9 = 43/43**；前后端语法通过。
- **坑与教训**：
  - **同一指标在不同页面用了不同算法 → 数字对不上，用户立刻会发现**。
    我在图例用「首尾差值」、在分组用「`rpChange` 累加」，同一个「S26 净 RP」竟给出
    `+8,843` 和 `−139` 两个数。**凡是重复展示同一指标，必须先统一口径并说明来源。**
  - **SVG 里的字号要按「缩放后的落地像素」来定**，不能直接照搬 CSS 像素。
    卡片宽度固定时缩放系数是常数（≈0.90），11px 落地只有 9.9px。
  - **新增可视化元素要考虑「太窄就没意义」的情况**。首尾两个赛季的区段可能只有几个点，
    硬画标签会重叠 —— 加宽度阈值跳过，比事后调位置更可靠。
  - 只靠 `innerText` 断言「图例顺序」容易误判：图例是**升序**、分组是**降序**，
    我的比对脚本一度因此报「全部不一致」，实际是脚本把两边顺序弄反了。
    **比对顺序敏感的数据时，先按 key 对齐再比。**

### 按赛季分组对局 + 显示赛季时间（含一处段位图 URL bug 修复）
- **类型**：功能 / 修复
- **文件**：`track.mjs`、`web/index.html`、`README.md`
- **改动**：
  - **`track.mjs`：内置赛季时间表**
    - ⚠️ **tracker.gg 不提供赛季起止时间**（实测其数据里只有 `currentSeason: 30` 这个数字，
      以及各赛季的 kills/wins 汇总）。所以内置 `SEASONS` 表（1~30 季，含开始日期与主题名），
      来源为维基百科中文版《Apex 英雄》赛季表（已核对）。
    - 新增 `seasonOf(ts)`：时间戳 → `{ season, name, start, end, split }`。
      split 按赛季中点划分（Apex 赛季约 3 个月，分上下半段），口径对齐 ALS 的 `s29_s2`。
    - TRN 会话与 ALS 逐场均标注 `season / seasonName / seasonStart / seasonSplit`。
      ALS 侧用它自己的 `split` 字段（如 `s29_s2`）解析，**比按时间戳推断更权威**。
  - **`web/index.html`：按赛季分组 + 赛季筛选**
    - 新增 `groupBySeason()`（赛季降序）与 `seasonGroupsCount()`。
    - 组头改为：`S30 · 诸神烙印 · 2026-08-05 起 · 14 个会话 · 排位 8 / 未定 6 · 净 RP +567`，
      下方一行 `08-10 12:32 → 09-12 04:15 · 第 1 段`（组内时间范围 + split）。
    - 新增**赛季筛选**（只列出真正有数据的赛季，带数量），**可与类型筛选叠加**。
    - 汇总行把「总时长」（TRN 没有）换成「赛季 N 个」。
    - Current Rating 卡新增**峰值 RP**：
      `历史峰值 24,879 RP · 本赛季 12,464`（tracker.gg 独有，ALS 没有）。
  - **修复：段位徽章 URL 拼接错误**
    - 现象：出现孤儿请求 `api.mozambiquehe.re/assets/ranks/Diamond%204.png` → 超时失败。
    - 原因：`rankImg(tier)` 直接拼字符串，而 **tracker.gg 的段位名是 `"Diamond 4"`（带空格）**，
      拼出 `Diamond 4.png`；ALS 侧是 `diamond4`（无空格）所以一直没暴露。
    - 修法：① `rankImg()` 归一化（`toLowerCase().replace(/[^a-z0-9]/g,'')`）；
      ② `rankBlock()` **优先用后端给的 `rankTierImg`**（tracker.gg 的 trackercdn，与段位名严格对应）；
      ③ 把 `rankTierImg` / `rankTierExact` / `peakRankScore` / `lifetimePeakRankScore`
      补进 `record`（之前只在 `--json` 的 `trn` 块里，前端拿不到）。
- **原因**：用户要求「查询赛季时间，区分排位数据」。
- **验证**：
  - **赛季推断（真实数据）**：87 个会话**全部**能归属赛季（0 个未知），
    分布 `S30·1:14 / S26·2:4 / S26·1:3 / S25·2:15 / S25·1:19 / S24·2:26 / S24·1:1 / S23·1:5`，
    与 tracker.gg 报的 `currentSeason: 30` **一致**（最新会话落在 S30）。
  - **页面赛季组头**（5 组，数据自洽）：
    · `S30 诸神烙印 · 2026-08-05 起 · 14 个会话 · 排位 8 / 未定 6 · 净 RP +567`
    · `S26 巅峰对决 · 2025-08-06 起 · 7 个会话 · 排位 6 / 未定 1 · 净 RP -139`
    · `S25 金童降世 · 2025-05-06 起 · 34 个会话 · 排位 19 / 未定 15 · 净 RP +9,036`
    · `S24 侵略 · 2025-02-11 起 · 27 个会话 · 排位 14 / 未定 13 · 净 RP +2,486`
    · `S23 源自裂缝 · 2024-11-05 起 · 5 个会话 · 排位 5 · 净 RP +5,525`
  - **时间范围校验**：每组范围都落在该赛季区间内
    （如 S30 是 `08-10 → 09-12`、S25 是 `05-19 → 08-04`），split 归属正确。
  - **筛选可叠加**：`全部 87 行` → 点 `S30` → `14 行` →
    再点「排位」→ `8 行` → 点 `S25` + 「全部」→ `34 行` → 点「全部赛季」→ 恢复 `87 行`。
  - **段位图**：修复后无孤儿请求；`trackercdn.com/.../diamond4.png` 加载成功
    （`naturalWidth = 120`），行内段位图 **87/87** 加载；段位名显示为罗马数字（`Diamond IV`）。
  - **峰值 RP**：Current Rating 卡显示 `历史峰值 24,879 RP · 本赛季 12,464`。
  - 横向溢出 0；前后端语法通过。
- **坑与教训**：
  - **「上游没提供」时要么内置、要么明确降级，不要假装有**。
    赛季时间是典型的「数据源不提供但业务需要」的字段 —— 我选择内置权威表并在 UI 上写明
    「按时间戳推导（tracker.gg 不提供赛季起止时间）」，避免后来者误以为是官方数据。
  - **同一字段在不同数据源里格式不同，拼接前必须归一化**。
    `Diamond 4` vs `diamond4` 这个差异在只用 ALS 时永远不会暴露，
    换成 TRN 后立刻变成 404。**跨源替换后要回归所有「拼字符串」的地方。**
  - **优先用数据源自带的资源，而不是自己拼**。TRN 已经给了 `rankScoreInfo.image`，
    却仍走「按段位名拼 URL」的老路 —— 既多做一次映射，又引入了格式风险。
  - 新字段要**沿着数据流检查每一层**：`rankTierImg` 在 `player` 里、`--json` 里都有，
    但 `record` 是显式列字段的白名单，漏了就传不到前端。

### 改为「只用 tracker.gg」：全部数据来自网页采集，不再依赖 ALS
- **类型**：功能 / 重构
- **文件**：`track.mjs`、`server.mjs`、`web/index.html`
- **改动**：
  - **采集脚本扩展**（`web/index.html` 的 `collectSnippet()`）：
    除 `standardSessions[0].items`（对局）外，**同时发送 `standardProfiles[0]`**（账号聚合 + 传奇分段）。
  - **`server.mjs`**：`ingestTrn` 接收并落盘 `data.profile`；摘要回传
    `hasProfile` / `level` / `legendCount`。
    （踩坑：第一版写成 `payload.profile`，实际在 `payload.data.profile`，见下。）
  - **`track.mjs`**：
    - 新增 `buildFromTrn({profile, sessions, sessionItems})`：从 tracker.gg 的 profile
      构建**与 ALS 同构**的 `player` / `legends` / `sessions`，上层逻辑无需改动。
    - 新增段位推导：`makeTierLookup()`（会话里的 `rankScoreInfo` → RP 最近邻映射，
      精确到 ±300 标记 `exact`）+ `tierFromScoreFallback()`（阈值表兜底，处理超范围的分数如历史峰值）。
    - 新增 `splitLevel()`：绝对等级 → 转生 + 等级（`1147` → `Prestige 2` + `Lv147`）。
    - **`provider` 默认值由 `als` 改为 `trn`**，且 `provider === 'trn'` 时
      **优先读浏览器采集落盘的数据**（无需 Key、不联网、瞬时），
      本地缺失时给出明确的采集指引而不是报错。
    - `--json` 新增 `trn` 块（`levelAbsolute` / `avatar` / `peakRankScore` /
      `lifetimePeakRankScore` / `rankTierImg` / `rankTierExact` / `season` / `activeLegend`）。
    - 玩家名改用 **Steam 别名**（`metadata.steamInfo.displayName` = `L-icet`），
      `platformUserHandle`（`bluekinger`）是 EA ID，两者都保留。
  - **`web/index.html`**：
    - 移除「来源切换」（只用 TRN），`loadMatches` 请求固定 `provider: 'trn'`。
    - 文案修正：`UID xxx` → 按是否为纯数字区分 **UID / EA ID**；
      常用传奇的「N 场」→「N 个会话」。
- **原因**：用户明确要求「数据现在只用 tkgg，不用 als」。
- **验证**：
  - **纯本地、不联网**：默认查询 **0.084 秒**返回（此前要 4~15s）。
  - **聚合战绩**（全部来自 tracker.gg）：`L-icet` / 等级 `147` + `Prestige 2` /
    RP `12,368` / 段位 **`Diamond 4`**（`rankTierExact: true`）/
    历史峰值 `24,879` / 本赛季峰值 `12,464` / 赛季 `30` / 当前传奇 `Octane`。
  - **生涯统计**：`Career Kills 6,487` / `Wins 210` / `Damage 3,017,021` /
    `Matches 656` / `Kills-Match 0.67` / `Damage-Match 309.78` / `Finishers 10`。
  - **传奇**：26 个分段，每个含 `BR Kills / BR Damage / BR Wins`。
  - **对局**：87 个会话，段位名覆盖 **87/87（100%）**；
    排位 `52` / 未定 `35`；净 RP `+17,475`。
  - **页面**：hero `L-icet`、`EA ID bluekinger`、`PRESTIGE 2`；
    `Diamond 4 / 12,368 RP`；Account `等级 147 / 转生 Prestige 2 / 传奇数 26 /
    追踪器 Kills, Wins, Damage`；对局 `87 个会话`、筛选条正常；横向溢出 0。
  - **回归**：三份离线测试 **17+11+9 = 37/37**；前后端语法通过。
- **坑与教训**：
  - **`payload.data.profile` vs `payload.profile`**：我在服务端按顶层取，
    实际嵌套在 `data` 下，导致 `hasProfile: false` 而误以为采集失败。
    排查靠「重启服务后重采 → 仍为 false → 回头核对路径」。
  - **🔴 发现并修复一处脏数据（真 bug）**：TRN 的**会话级 `stats.rankScore` 不可信**！
    实测同一会话：match 级 `12410`（Diamond 4）而会话级 `24055`；
    另一处 match 级 `11553`（Platinum 1）而会话级 `57571`。
    这会让 RP 走势图被拉出一个 **57.6k 的尖峰**、把正常范围压扁，并让段位推导出错。
    **修法：RP 与 RP 变化优先取 match 级**（`m0.stats.rankScore`），
    而 `kills/damage/wins` 仍取会话级（那才是时段合计，match 级常为 null）。
    修复后异常值 **2 → 0**，RP 范围收敛到 `3,573 ~ 12,715`。
  - **跨源字段的「权威性」要逐字段判断，不能整块信任**。
    同一份 TRN 数据里，`rankScore` 该用 match 级、`kills` 该用会话级 ——
    粒度混在同一对象里，凭直觉统一取值必然出错。
  - `provider` 默认值改了之后，**不带参数的老命令行为也会变**，
    这是个隐性破坏性变更；已在注释里写明 ALS 仍需显式 `--provider als`。

### 对局数据默认只采集 tracker.gg（ALS 的对局历史滞后）
- **类型**：功能
- **文件**：`track.mjs`、`server.mjs`、`web/index.html`、`README.md`
- **改动**：
  - **`track.mjs`**：新增 `--matches-source <trn|als|both>`，**默认 `trn`**。
    `provider === 'both'` 时按它决定是否抓 ALS 的 gameHistory。
    `--json` 输出新增 `matchesSource` 与 `matchesGranularity`，让前端知道拿到的是什么。
  - **`server.mjs`**：`query()` 透传 `matchesSource`。
  - **`web/index.html`**：
    - 默认 `matchesSource = 'trn'`，请求随带上。
    - 新增**来源切换**（`tracker.gg` / `ALS` / `两者`），切换时清缓存并重新请求。
    - `renderMatches(pack)` 按来源过滤：只用 TRN 时 `alsAll` 视为空 →
      `trnAll` 的「比 ALS 新」过滤自动全通过（`alsNewest = 0`）。
    - 新增 `trnAsMatches()` + `perMatch(pack)`：**走势图 / 常用传奇在 ALS 为空时用 TRN 兜底**
      （把会话的扁平字段适配成 `stats` 对象的形状），否则只采集 TRN 时这两个视图会空。
    - 汇总指标（净 RP / 最常使用）改为按**当前展示的全部记录**计算；
      「总时长」仅在有 ALS 逐场数据时显示（TRN 没有时长）。
    - 空状态区分「没采集过 TRN」（给出采集指引）与「真的没有记录」。
- **原因**：用户指出「只采集 tkgg 的数据，als 里没有战绩进行查询啊」——
  ALS 的对局历史滞后约 55 天，拿它当来源等于看不到近期战绩。
- **验证**：
  - **后端**：`{matchesSource:'trn', provider:'both'}` →
    `matches` **0 条**（确认没抓 ALS gameHistory）、`trnSessions` **87 条**、
    `compare.trnSource = browser`；**耗时 4.1s**（此前要 15s+，省掉了 gameHistory 的 1~3 个请求）。
  - **前端三种来源**（浏览器实测）：
    · `tracker.gg`（默认）→ 标题 `87 个会话`，汇总 `排位 52 / 未定 35 / 净 RP +17,756 /
      最常使用 艾许 10 个会话`，87 行全为 TRN 行；
    · `ALS` → `10 场逐场`，`排位 10 / 净 RP +571 / 总时长 2 小时 20 分 / 最常使用 瓦尔基里 7 场`，
      10 行 ALS、0 行 TRN；
    · `两者` → `14 个会话 + 10 场逐场`，24 行 = 14 TRN + 10 ALS，
      汇总 `排位 18 / 未定 6 / 净 RP +1,138 / 总时长 2 小时 20 分`。
  - 走势图与常用传奇在 TRN 模式下仍有数据（兜底生效）；横向溢出 0；前后端语法通过。
- **坑与教训**：
  - **只换数据源会连带打断其他视图**。走势图与常用传奇原本依赖 ALS 逐场，
    一旦不再抓 ALS 就会变空 —— 所以必须同时做**字段形状适配 + 回退**，
    而不是只改一处请求参数。改「数据来源」时要顺着数据流把所有消费方都过一遍。
  - **「只采集 TRN」有个固有代价**：TRN 只有会话级聚合，**没有地图与时长**。
    界面上必须写清楚（已在 TRN 组头下标注），否则用户会以为数据丢了。
  - 省请求的收益很直观：跳过 ALS 的 gameHistory，单次查询 **15s+ → 4.1s**。

### 排位/匹配分类的端到端验证（补齐上次未能验证的部分）
- **类型**：验证
- **文件**：无改动（纯验证），结论回填到上一条的「未验证」项
- **改动**：无。上次因 ALS 网络不通，真实数据的 `playlist` 分布只做了离线 fixture 验证；
  本次网络恢复后补齐。
- **原因**：用户要求「重新验证」。
- **验证**：
  - **ALS 真实数据（230 场 / s29_s2 整季）**：
    `playlist` 分布 **`{"ranked":218,"pubs":12}`**，**匹配占比 5%**
    （与离线推导的 s29_s2 约 4% 吻合）；
    与「有无 RP」的交叉验证 **230/230 完全一致**。
  - **匹配场确实被正确识别**（12 场全部列出），且特征符合预期：
    `rp = null`、`mode` 为 `Unknown` 或 `Battle Royale`（**均无 `BR` 粗体标记**）；
    样例 `Rampart / Unknown`、`Seer / Battle Royale / Worlds Edge`、`Axle / Unknown`。
  - **最近 60 场全为排位**（`ranked 60 / pubs 0`）—— 说明匹配场集中在更早的时段，
    这与 230 场里 5% 的结论不矛盾；也解释了为什么只拉 10 场时看不到匹配。
  - **页面筛选（真实数据，24 条 = 10 ALS 逐场 + 14 TRN 会话）**：
    `全部 24*` → 点「排位」→ **18 条**（18 个排位徽标、净 RP +571、时长 2 小时 20 分、
    18 行带 `.is-ranked` 琥珀侧线）→ 点「匹配」→ **6 条**（全为 TRN 未定、净 RP 0、时长 —）
    → 点「全部」→ 恢复 24 条。
    筛选条文案：`类型 全部 24 | 排位 18 | 匹配 6` + 判定依据说明。
  - **超时保护复测**：第 1 次查询 15s 成功（ALS 已恢复）；第 2 次 3s 成功；
    `track.mjs` 残留进程 **0**。
  - **离线测试回归**：`verify-als-playlist` **17/17**、`verify-playlist` **11/11**、
    `verify-trn-page` **9/9** —— 合计 **37/37**。
  - 前后端语法检查通过；桌面 1500px 横向溢出 0。
- **坑与教训**：
  - **「拉 60 场看不到匹配」不等于功能坏了**。分布本身不均（5%），
    且集中在较早时段；验证分类功能必须**拉足够覆盖多时段的样本**，
    否则会得出错误结论（我一度怀疑是分类逻辑有问题）。
  - **网络恢复后要主动回填「未验证」清单**。上次诚实标注了未验证项，
    这次一恢复就补齐，比含糊带过要可靠。
  - DNS 能解析（`apexlegendsstatus.com → 198.0.6.128`）但 TCP 连不上，
    对照多站点（百度 52ms 正常、GitHub/ALS/tracker.gg 全超时）可判定是
    **本机到境外的链路问题**，不是站点故障、也不是代码问题。

### 区分排位 / 匹配对局（并修复「查询卡住锁死服务」）
- **类型**：功能 + 修复
- **文件**：`track.mjs`、`server.mjs`、`web/index.html`、
  `data/fixtures/als-gamehistory.sample.json`（新增）、
  `data/fixtures/verify-als-playlist.mjs`（新增）、`data/fixtures/verify-playlist.mjs`（新增）
- **改动**：
  - **后端：推导 `playlist` 字段**
    - ⚠️ **ALS 不直接标注排位/匹配** —— 它把排位赛也写成 `mode: "BR"`（那只是游戏模式）。
      可靠判据：**Apex 只有排位赛才产生 RP**。有 `rp` → `ranked`；无 → `pubs`。
    - 实测交叉验证（s29_s2 / s29_s1）：有 RP 221/193 场且 `bold` 均含 `BR`；
      无 RP 9/53 场且 `bold` **全都不含** `BR` —— 两个信号完全一致。
    - TRN 侧用 `rankScoreChange`：非 0 → `ranked`；**0 → `unknown`**
      （实测 `前值 + change == 当前值` 在 8/8 非零样本上成立，证明它是真实增量；
      但 `= 0` 无法区分「匹配」与「净变化为 0 的排位」，所以**不写成「匹配」**）。
  - **前端：徽标 + 筛选 + 统计**
    - 行内徽标：`排位`（琥珀底）/ `匹配`（钢青描边）/ `未定`（灰，带 tooltip 说明）；
      排位行左侧加一条琥珀细线（`.mrow.is-ranked::before`），扫一眼即可分类。
    - 汇总行改为 `排位 N / 匹配·未定 N/N / 净 RP / 总时长 / 最常使用`。
    - 新增筛选条 `全部 / 排位 / 匹配`（含各自数量）。
      **「匹配」视图包含「未定」**，否则那些行两个视图都看不到。
    - 点筛选**只重渲染卡片、不重新请求**（`bindMatchFilter`）。
  - **修复：查询卡住会把服务锁死**
    - 现象：ALS 不通时一次查询挂 3 分钟以上，`server.mjs` 的 `busy` 一直被占住，
      后续所有查询都返回「上一个查询还在进行中」（实测踩到，且当时还有个
      `track.mjs` 子进程残留）。
    - `track.mjs`：`fetchWithRetry` 加**显式超时** `AbortSignal.timeout`（默认 15s，
      `APEX_HTTP_TIMEOUT_MS` 可调）。必须显式设 —— Node 的 fetch 只在建连阶段有超时，
      连上后对方不返回可能挂到 headers timeout（默认 300s）。
    - `track.mjs`：**区分两类失败的重试策略** ——
      「连不通/超时」（整站不可达）最多试 2 次就放弃；「429/5xx」仍按退避试 4 次。
    - `track.mjs`：把底层错误翻译成可操作的中文，不再只丢一句 `fetch failed`。
    - `server.mjs`：`runTrack` 加**硬超时 60s**（`APEX_TRACK_TIMEOUT_MS` 可调），
      超时强杀子进程并返回提示 —— 保证 `busy` 一定会释放。
    - 前端：新增 `fetchJson()` 统一带 75s 超时，超时给出明确文案而不是无限转圈。
- **原因**：用户要求「区分匹配和排位对局」。
- **验证**：
  - **ALS 侧分类（离线，`verify-als-playlist.mjs`）**：**17/17 通过** ——
    排位场（Valkyrie/Rampart，有 RP）→ `ranked`；匹配场（Wraith/Alter，无 RP）→ `pubs`；
    「有 RP ⟺ ranked」不变式成立；`legend/duration/durationSec/rank/rpChange±/map/level/mode`
    等原有字段未被破坏；空输入与 `no games have been recorded` 均返回 `[]`。
  - **TRN 侧分类（离线，`verify-playlist.mjs`）**：**11/11 通过** ——
    真实落盘数据 87 会话 → `ranked 52 / unknown 35`；
    `playlist` 与 `rpChange` 严格一致（0 处不符）；边界（chg>0 / chg<0 / chg=0 / chg 缺失）全部正确。
  - **前端筛选（浏览器，合成数据）**：全部 6 行 → 点「排位」剩 3 行（3 个排位徽标、
    净 RP +21、时长 24 分）→ 点「匹配」剩 3 行（未定+匹配+匹配、净 RP 0、时长 17 分）→
    点「全部」恢复 6 行；`is-ranked` 行计数与琥珀侧线（`rgb(255,159,40)`）均正确。
  - **修复效果**：ALS 不通时单次查询失败耗时 **46.9s → 21.9s**；
    连续两次查询，第 1 次 04:20:08 → 04:20:29 返回
    「连接 apexlegendsstatus.com 超时（15s）。请检查网络后重试。」，
    **第 2 次立刻返回同样错误**（而非「上一个查询还在进行中」）→ 证明 `busy` 已释放。
  - **回归**：`verify-trn-page.mjs` **9/9 通过**；三份离线测试合计 **37 条断言全过**；
    前后端语法检查通过。
- **⚠️ 未验证**：**ALS 全链路的真实数据验证没做成** —— 过程中 ALS 恰好网络不通
  （`apexlegendsstatus.com` 连接超时，百度正常，属出口路由问题），
  所以「真实 API 返回里 `playlist` 的分布」只做了离线 fixture 验证，未跑通真实端到端。
  网络恢复后应复核：拉 60 场看 `ranked`/`pubs` 实际占比是否符合预期（s29_s2 约 4% 为匹配）。
- **坑与教训**：
  - **测试失败先怀疑自己的测试**。`rank`/`map`/`level` 三条断言首轮失败，
    表面像产品 bug，实际是**我的 fixture 结构不对**：
    ① `rank` 的路径必须含 `ranks/`（正则是 `ranks\/([a-z0-9]+)\.png`），我写成了 `ranks-new/`；
    ② `Map played`/`Level` 来自**详情块**（以 `<div id="MMv2-` 开头、由 `parseGameHistory`
       切出来的兄弟节点），不是 game 块本身。
    corpus：fixture 必须**结构忠实**，否则测出来的是自己的臆想。
  - **「上游不标注」时不要假装有权威字段**。`mode: "BR"` 很容易被误当成模式区分，
    但实测排位/匹配都是 `BR`。真正的判据是**副作用**（只有排位才产生 RP）。
    对 TRN 更保守：`change == 0` 标成「未定」而不是「匹配」——
    宁可显示弱一点，也不要给出错误断言。
  - **长驻服务的「忙标志」必须有兜底**。`busy` 这类自旋锁一旦因为子进程挂住而没释放，
    整个服务就不可用了；**给外部调用加强制超时**是最便宜的保险。
  - Node 的 `fetch`（undici）**只在建连阶段有超时**，连上后不返回会一直等到
    headers timeout（默认 300s）—— 对不可达/半死的上游，必须自己设 `AbortSignal.timeout`。

### 对局记录改为「ALS 逐场 + TRN 较新会话」合并显示
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - `loadMatches()` 请求改为 `provider: 'both'`，同时拿到 `matches`（ALS 逐场）
    与 `trnSessions`（TRN 会话）；缓存结构由数组改为 `{ als, trn }`。
  - 新增 `trnRow(s)`：渲染 TRN 会话行 —— 传奇头像/名 + **「会话」徽标** +
    `N 场 · 会话合计` + 击杀/伤害 + ±RP + RP。
  - `renderMatches(pack)` 重构为两段：
    **① TRN 组置顶**（附来源说明：会话是连续游戏的合计、非单场、无地图与时长）；
    **② ALS 组**（原有分组，保留逐场明细与第二行 `matchExtra`）。
  - 汇总行改为「逐场 N / 净 RP / 总时长 / **较新会话 N** / 最常使用」。
  - **只保留比 ALS 最新一场更新的 TRN 会话**（`timestamp > alsNewest`），
    避免同一段时期被两个源各显示一遍。
  - 走势图与「常用传奇」**仍只用 ALS**（`pack.als`）—— TRN 是会话聚合，
    粒度不同，混进去会让曲线与占比失真。
  - 样式：`.badge-sess`、`.mrow.trn`（琥珀渐变底 + 降低对比度）、
    `.mh-split .sp.trn`、`.src-note`。
- **原因**：用户明确最终需求 ——「希望在**对局记录**里显示」（看到最新对局，
  而不是只在「数据对比」页）。
- **验证**：
  - 卡片标题 `对局记录 10 场逐场 + 14 个较新会话`；汇总
    `逐场 10 / 净 RP +571 / 总时长 2 小时 20 分 / 较新会话 14 TRN / 最常使用 瓦尔基里 7 场`。
  - 分组顺序正确：`TRN 09-12 04:15 · 14 个会话 · 较新` → `ALS 逐场明细 · 10 场`
    → `S29 · 第 2 段 07-19 03:21 · 10 场 · 净 RP +571`。
  - 行数：TRN `14` 行、ALS `10` 行、ALS 明细块 `.extra` `10` 个；
    TRN 行带「会话」徽标 14 个；首行 `动力小子 [会话] 1 场 · 会话合计 击杀 254 伤害 82,760`。
  - **未被污染**：常用传奇仍是 `3 个传奇`（瓦尔基里/兰伯特/探路者）、
    走势图仍是 `10 个` ALS 数据点。
  - 头像 `24/24` 加载；桌面横向溢出 0（在 1500px 下）。
- **坑与教训**：
  - **TRN 的数值远比 ALS 宽**（TRN 会话伤害可达 7 位 `279,773`，ALS 逐场只有 3~4 位）。
    原来按 ALS 定的 `66px` 定宽格放不下，**「伤害」标签被压成竖排**（「伤/害」两行）。
    修法：格宽 66 → 96px、列宽 152 → 210px，并给格加 `white-space:nowrap`。
    这类「合并两个来源」的场景，**布局要按最宽的那个来源取余量**。
  - `.ks` 的对齐方式要从 `justify-content:flex-end` 改成 `flex-start`：
    每行的格数不固定（有的会话只有击杀、没有伤害），`flex-end` 会让单格行里的
    「击杀」跑到最右，跨行标签位置就对不上了。改用固定容器宽 + 左对齐，
    首格位置恒定，标签始终纵向对齐（实测击杀左边缘恒为 910px、伤害 1020px）。
  - **合并展示必须先判断粒度是否兼容**。TRN 会话的 `kills` 是时段合计，
    若当作逐场塞进走势图/常用传奇，会得到完全错误的结果 ——
    所以只在对局记录里并列展示，且加徽标与来源说明，其余页面继续只用 ALS。

### 复测免 Key 采集：发现「改了代码但服务没重启」
- **类型**：修复
- **文件**：`server.mjs`（仅重启验证，未改代码）
- **改动**：
  - 重新执行一次浏览器采集，端到端复测免 Key 链路。
  - **重启 `server.mjs`** —— 这是本次的关键动作（原因见下）。
- **原因**：用户回复「好」（同意重新采集），复测时发现采集返回的
  `newestLegend` 仍是数字 `9`，而不是上一轮已修好的 `"Octane"`。
- **验证**：
  - **定位过程**：`grep` 确认 `server.mjs` 里 `legendName()` 取的是
    `displayValue`（代码是对的）；再比对**进程启动时间 vs 文件 mtime** ——
    服务启动于 `03:58:54`，而 `server.mjs` 修改于 `03:59:45`。
    → 服务跑的是**修改前的旧代码**，所以修复没生效。
  - 重启后重新采集：`{ok:true, sessionCount:87, newestLegend:"Octane"}` ✅
  - 页面复测：新鲜度框显示「TRN · 浏览器采集 · 英雄 动力小子 · 采集于 09-14 04:01」；
    结论条 `[ok]`「TRN 的对局比 ALS 新约 55 天；当前 RP、等级（绝对值）两源一致」；
    交叉验证两行皆「一致 ✓」；覆盖度 `10 逐场 / 87 会话`；页签 0 禁用；横向溢出 0。
  - 接口自检：空 body POST `/api/ingest/trn` →
    `{"ok":false,"error":"缺少 data.items（应为 tracker.gg 的 standardSessions[0].items）"}`，
    校验按预期拒绝。
- **坑与教训**：
  - **改完代码必须重启服务，否则验证的是旧行为**。Node 不会热重载模块，
    这次就因此误以为修复无效。**跑验证前先比对「进程启动时间」与「文件 mtime」**，
    这是个值得固化成习惯的检查：
    `ps -o lstart -p $(lsof -nP -iTCP:8787 -sTCP:LISTEN -t)` 与 `ls -lT <file>`。
  - 反过来看，**「修复无效」的第一怀疑对象应当是「改动是否真的生效」**，
    而不是代码逻辑又错了 —— 先排除环境，再怀疑实现。

### 免 Key 采集 TRN：浏览器采集 → 本地落盘 → 页面展示
- **类型**：功能
- **文件**：`track.mjs`、`server.mjs`、`web/index.html`、`README.md`
- **改动**：
  - **`track.mjs`**
    - 新增 `extractInitialState(html)`：从 HTML 抽 `window.__INITIAL_STATE__`。
      不能用正则 —— 那段 JSON 300KB+ 且含大量嵌套与转义，必须**按括号配对扫描**
      并正确处理字符串内的 `{}` 与转义引号。
    - 新增 `fetchTrnPage()`：直接 GET `apex.tracker.gg/.../matches` 并解析会话数据。
    - 新增 `readTrnSessionsFile()`：读回浏览器采集落盘的 `data/trn-sessions.json`。
    - **TRN 数据源回退链**：`官方 API`（配了 Key）→ `网页直抓` → `浏览器采集落盘`。
      结果里用 `trnSource` 标明来源（`api` / `page` / `browser`），并记录 `trnCollectedAt`。
    - 限流拆成两个（配额不同，不能混用）：
      `TRN_API_LIMITER`（官方 API 10次/分钟，用 8）与
      `TRN_WEB_LIMITER`（网站 20次/分钟，默认 18，可用 `TRN_WEB_RPM` 覆盖）。
  - **`server.mjs`**
    - 新增 `POST /api/ingest/trn`：接收浏览器采集的数据，落盘 `data/trn-sessions.json`。
    - 新增 `GET /api/trn-sessions`：读回该文件。
    - 加入 **CORS**（`Access-Control-Allow-Origin: *` + `OPTIONS` 预检）——
      因为要从 tracker.gg 页面跨源 POST 到本地服务。
      服务只监听 `127.0.0.1`，不暴露到局域网。
    - 采集接口的请求体上限放宽到 16MB（原始页面数据可能数 MB）。
    - `readBody(req, max)` 参数化上限。
  - **`web/index.html`**
    - 「数据对比」页新增「**怎么更新 TRN 数据（无需 Key）**」区块：
      三步指引 + **可一键复制的采集脚本**（`collectSnippet()` + 复制按钮）。
    - 新鲜度框显示数据来源（`网页直抓` / `官方 API` / `浏览器采集`）与采集时间。
    - 能力矩阵里「需要 API Key」改为**两边都是「无」**。
    - 文案改为说明 Cloudflare 拦截与浏览器采集方案。
  - **`README.md`**：重写第 4 节 —— 为什么不需要 Key、为什么必须浏览器采集、
    采集步骤、限额、两源强项对照。
- **原因**：用户质疑「不是，为什么非要用 key」，并给出思路：
  「打开网页，输入 id，在 Recent Matches 下就有战绩，采集数据然后显示，就这么简单，
  tracker.gg 对访问有限制，添加 1 分钟只能访问网站 20 次」。
- **验证**：
  - **Cloudflare 拦截实测**（这是方案设计的依据）：
    · Node 抓页面 HTML（简洁头）→ **403**，`title: Just a moment...`（挑战页）
    · Node 抓页面 HTML（完整 `sec-ch-ua` 指纹头）→ **403**，同样挑战页
    · Node 抓 `api.tracker.gg`（网站内部接口，非 public-api）→ **403**，`You've Been Blocked`
    · 真实浏览器 → ✅ 正常
    → 结论：**服务端抓不到，只有过挑战的浏览器能拿数据**。
  - **解析器单测**（`data/fixtures/verify-trn-page.mjs`，**无需 Key**）：**9/9 通过**
    —— 含字符串里的 `{}` 与转义引号、截断输入、空输入、21KB payload(1ms)。
    失败态一律返回 `null` 而不抛异常。
  - **浏览器采集实测**：在已打开的 tracker.gg 页面执行采集脚本 →
    `POST /api/ingest/trn` 返回 `{ok:true, sessionCount:87,
    newest:"2026-09-11T20:15:02.617887Z", newestLegend:"Octane"}`；
    落盘 `data/trn-sessions.json`（545KB，87 会话）。
  - **全链路 `--provider both`**：`trnSource: browser`、`trnError: null`；
    ALS 最新 `2026-07-18`(Valkyrie) vs TRN 最新 `2026-09-11`(**Octane**)、
    **相差 55 天**；交叉验证 `当前 RP 12368 = 12368` ✅、`绝对等级 1147 = 1147` ✅；
    结论 `[ok]`。
  - **页面实测**：来源显示「浏览器采集 · 采集于 09-14 03:59」、差距提示、
    交叉验证两行「一致 ✓」、可复制脚本与复制按钮存在、横向溢出 0。
  - 前后端语法均通过。
- **坑与教训**：
  - **我一开始答错了方向**。用户问「为什么非要用 key」，我先前一直在推 API Key 方案，
    没意识到「抓网页」本身是可行路线 —— 而我**早就掌握关键证据**：
    浏览器控制台里 `api.tracker.gg` 因 CORS 全部失败、但页面数据照常显示，
    这已经说明数据嵌在 HTML 里。**证据在手上却没推论到底**，绕了很远。
  - **Cloudflare 让「服务端刮网页」这条路彻底不通**。加 `User-Agent`、
    `sec-ch-ua` 全套指纹都没用，返回的是 JS 挑战页而非数据。
    所以「浏览器采集 + 回传本地」不是绕路，而是**唯一可行**的免 Key 方案。
  - `api.tracker.gg`（网站内部）与 `public-api.tracker.gg`（开发者）是**两套不同接口**，
    别混淆：前者要过 Cloudflare，后者要 Key，**两者都不是免 Key 通路**。
  - CORS 只约束浏览器、不约束服务端 —— 但反过来，
    **服务端无 CORS 限制不等于能访问**：Cloudflare 在网络层就拦掉了。
    这条容易搞混。
  - 从 HTTPS 页面 POST 到 `http://127.0.0.1` 是允许的（localhost 被视为可信来源），
    所以浏览器采集脚本能直接回传，不需要 HTTPS 隧道。
  - `legend` 字段的 `value` 是**数字 ID**、名字在 `displayValue`；
    摘要里直接取 `value` 会显示成 `9` 而不是 `Octane`（我第一版就踩了这个）。

### 实现方案 C（ALS+TRN 混合）与跨源对比分析
- **类型**：功能
- **文件**：`track.mjs`、`server.mjs`、`web/index.html`、`README.md`、
  `data/fixtures/trn-sessions.sample.json`（新增）
- **改动**：
  - **后端 `track.mjs`**
    - 新增 `normalizeTrnSessions()`：把 TRN `/sessions` 归一化成与 ALS 对局同构的条目，
      便于前端复用渲染。兼容两种形状（`data.items[]` / `data.segments[]`）。
    - 新增 `buildComparison()` + `buildVerdict()`：产出新鲜度、交叉验证、能力矩阵、
      覆盖度与结论。
    - 新增 `alsAbsoluteLevel()`：对齐两源等级口径
      （ALS 是「等级 + 转生」两段，TRN 是绝对等级；`Lv147 + Prestige 2 → 1147`，与 TRN 一致）。
    - 新增 `--provider both`：ALS 聚合 + ALS 逐场 + TRN 会话 + 对比结果；
      TRN 缺 Key 时**不报错**，降级为纯 ALS 并在 `compare.trnError` 里说明。
    - `--json` 输出新增 `trnSessions` 与 `compare` 两个字段。
    - 新增 **`.env` 自动加载**（`process.loadEnvFile`，带「不覆盖已有环境变量」保护），
      配置 Key 只需建 `.env` 并重启，不必 `export` 或加 `--env-file`。
    - `.env` 已在既有 `.gitignore` 中 —— 密钥不会被提交。
  - **后端 `server.mjs`**：`query()` 接受并透传 `provider`（`trn` / `both`）。
  - **前端 `web/index.html`**：新增 `SHOW_COMPARE` 开关、「数据对比」页签与 `#pageCompare` 页面；
    `renderCompare()` / `loadCompare()` / `tsText()`；`.verdict` / `.fresh` / `.ctab` / `.sub-h` 样式。
    **只在点了该页签时才发请求**（TRN 限流 10 次/分钟），且只取 10 场对局。
  - **`README.md`**：新增「方案 C」小节（两源强项对照表、TRN 会话级聚合的坑、`.env` 配置方式）。
  - **`data/fixtures/trn-sessions.sample.json`**：TRN `/sessions` 的**真实响应样本**
    （从 tracker.gg 页面内嵌数据提取），用于在**没有 Key** 的情况下验证解析与对比逻辑。
- **原因**：用户选择方案 C（混合），并要求对两个数据源做对比分析。
- **验证**：
  - **（无需 Key）归一化**：用真实 fixture 跑 `normalizeTrnSessions` →
    3 条会话，字段齐全；断言全通过：`granularity` 全为 `session`、`duration` 全为 `null`（已弃用）、
    `timestamp` 全部解析成功、最新会话英雄 = `Octane`。
  - **（无需 Key）对比渲染**：向 `renderCompare` 喂合成 compare（TRN 可用）→
    结论条、新鲜度双框（ALS `07-18 23:21` 瓦尔基里 / TRN `09-13 07:55` 动力小子）、
    差距提示「相差约 56 天」、交叉验证三行（`12,368 RP` 一致 ✓ / `1,147` 一致 ✓ /
    场均伤害 `512 vs 640` 不一致 ✗）、覆盖度 `230 逐场 / 87 会话` 全部正确渲染。
  - **（端到端，无 Key）**：浏览 `数据对比` 页签 →
    服务端确实跑通了 `--provider both` 全链路（ALS 抓取 + 对比生成 + 前端渲染），
    页签 `Overview / 常用传奇 / 数据对比 / 对局记录`，0 禁用；
    TRN 分支按设计降级并给出 `.env` 配置指引。
  - **`.env` 加载**：临时 `.env` 写入 `APEX_ENV_TEST=loaded-ok` → 加载成功；
    且「先设环境变量再加载」时**不被覆盖**（保持 `from-shell`）。测毕已删除临时文件。
  - 语法：`node --check track.mjs` / `server.mjs` 通过；前端 `<script>` 用 `new Function` 校验通过。
- **⚠️ 未验证的部分（诚实标注）**：**TRN 的真实网络请求路径未跑过** ——
  没有 `TRN_API_KEY`，无法实际调用 `/sessions`。已验证的只有
  「归一化逻辑（真实 fixture）」「渲染（合成数据）」「缺 Key 时的降级」。
  拿到 Key 后需要复核：TRN 实际返回是否与 fixture 同形、`items[]` 与 `segments[]` 哪个是真的。
- **坑与教训**：
  - **靠猜 schema 写解析器很危险，所以先用真实数据取样**。我没有 Key，
    于是从 tracker.gg 已加载的页面里提取 `window.__INITIAL_STATE__.stats.standardSessions`
    —— 那是**真实响应**，比臆测强得多。取样后立刻发现两个反直觉的事实（见下）。
  - **发现 1：TRN 的 `/sessions` 是会话级聚合，不是逐场值。**
    证据：`items[].stats` 与 `items[].matches[].stats` **恒等**（实测 12 个会话全部相同），
    且量级不可能是单场（单会话 254 杀 / 82,760 伤害）。
    所以不能用它冒充逐场明细 —— 已打 `granularity:'session'` 并在 UI 明确说明。
  - **发现 2：TRN 的 `duration` 恒为 `"00:10:00"`**，8 个会话全部相同 → 该字段不可信，
    归一化时直接置 `null`，不进入任何计算。
  - 两源等级口径不同（ALS「147 + Prestige 2」vs TRN「1147」），
    **直接比对会误判为不一致**。必须先做口径对齐（`+1000 × (prestige-1)`）再比。
  - `data/fixtures/` 目录是被 `.gitignore` 覆盖的吗？确认没有 —— fixture 要入库，
    它不含任何密钥（只是游戏数据），对后来者复现解析逻辑很有价值。

### 🔎 查明「本地显示瓦尔基里、tracker.gg 显示动力小子」的原因
- **类型**：诊断
- **文件**：无（只读排查，未改代码）
- **用户现象**：tracker.gg 最近一局是**动力小子(Octane)**，本地对局记录最新却是**瓦尔基里(Valkyrie)**。
- **结论**：**代码与抓取逻辑均正确；差异来自数据源 —— ALS 的对局历史对这名玩家滞后约 2 个月。**
- **验证**（全部基于原始响应，非我们解析后的结果）：

  | 项目 | ALS（我们用的） | tracker.gg |
  |---|---|---|
  | 最新会话 | `1 month 26 days ago`（≈ 07-19） | `1 day ago` |
  | 最新英雄 | 瓦尔基里 | 动力小子 Octane |
  | 最新赛季有数据的 | `s29_s2` | Season 30 |
  | `s30_s1` / `s30_s2` | **284 字节，`no games have been recorded`** | 有 |

  - `parseSplits` 反转后最先抓的是 `s30_s2 → s30_s1 → s29_s2 → s29_s1`，**顺序正确**：
    选项原顺序 `s10_s1 … s30_s2`（旧→新），`reverse()` 后即为新→旧。
  - **带 cookie 的完整流程**（与 `fetchAls` 一致）复测，结论相同：
    `s30_s2`/`s30_s1` 均是 284 字节空响应，`s29_s2` 964KB / 230 场 / 最新会话 `1 month 26 days ago`。
    → 排除了「没带 cookie 导致拿不到新数据」的可能。
  - **关键旁证：ALS 的聚合数据其实是最新的**
    —— ALS 报 `level 147 (Prestige 2)`，tracker.gg 报 `Level 1,147`，
    两者一致（147 + 1000 = 1147）；RP 也都是 `12,368`。
    说明 ALS 的**聚合数据走官方渠道、实时**，而**对局历史走另一套采集、滞后**。
- **可选项**（未实施，待用户决定）：
  1. 接受现状 —— ALS 免费、无需 Key，只是对局停在上一次被采集到的场次；
  2. 配 `TRN_API_KEY` 用 `--provider trn` 拿最新对局（与 tracker.gg 同源，限流 10 次/分钟，
     已由 `TRN_LIMITER` 保护）；
  3. 混合：聚合数据用 ALS、对局用 TRN。
- **坑与教训**：
  - **同一玩家、同一时刻，不同数据源的对局历史可能差几个月**。
    排查「数据对不上」时，必须**分别核对每个源自己的原始响应**，
    不能拿 A 源的结果去质疑 B 源的实现。
  - **「聚合数据新 + 对局数据旧」可以在同一站点内并存**（ALS 就是如此），
    因为两者来自不同采集渠道。所以「等级/RP 是对的」并不能推出「对局历史也是对的」，
    排查时要分开验证。
  - 一开始我怀疑是 `parseSplits` 的 `reverse()` 把新旧顺序弄反了 —— 这是合理的怀疑方向，
    但实测证明顺序正确。**怀疑要落到实测上**，否则容易改错地方。

### 撤销误加的「本地存档」页面，并查明对局数据上限
- **类型**：重构
- **文件**：`web/index.html`
- **改动**：
  - 完全移除上一轮误加的「本地存档」页面：`SHOW_ARCHIVE` 开关、`#pageArchive`/`#archiveCard`、
    `tabArchive` 页签、`renderArchive()`/`loadArchive()`/`collectBar()`/`bindCollect()`/
    `stampText()`/`deltaOf()`、`.arow`/`.snaps`/`.collect-bar` 样式，
    以及 `showPage()` / `goto()` / `tabReady()` 里对应的分支。共删除 203 行（JS 159 + CSS 44）。
  - 恢复后：页签回到 `Overview / 常用传奇 / 对局记录`，`showPage()` 只切两页。
- **原因**：用户澄清「我只是让你更新对局记录而已」—— 上一轮把需求理解成「建一个本地存档页」，
  实际只需要对局记录反映最新数据。多做的功能应撤掉，避免留下无人使用的代码。
- **验证**：
  - 浏览器实测：页签 `Overview / 常用传奇 / 对局记录`，禁用数 0；
    `#pageArchive` 与 `#archiveCard` 均不存在；横向溢出 0。
  - 对局记录正常：`MATCHES 对局记录 10 场`、分组头 `S29 · 第 2 段 · 07-19 03:21`、
    10 行、第二行字段齐全；常用传奇页可正常切换（3 行）。
  - `grep` 确认无 `SHOW_ARCHIVE/ARCHIVE_HEAD/collectBar/loadArchive/archiveCard/pageArchive/
    tabArchive/archiveInflight/stampText/deltaOf/.arow/.snap` 残留。
  - JS 语法检查通过；11 个关键函数（`doQuery`/`render`/`loadMatches`/`renderPicks`/
    `renderTrend`/`renderMatches`/`buildTabs`/`goto`/`showPage`/`pickStats`/`matchExtra`）均在。

### 🔎 查明：对局记录停留在 07-19 是数据源限制，不是解析 bug
- **类型**：诊断
- **文件**：无（只读排查）
- **结论**：**ALS 侧确实没有 07-19 之后的任何对局数据**，页面对局记录已经是最新可得的。
- **验证**（均为原始 HTML 层面，非我们解析后的结果）：
  - `core/gameHistory?split=s30_s2` 与 `s30_s1` 都返回 `no games have been recorded`
    （响应仅 284 字节），而 `s29_s2` 返回 964KB / 12 个会话 / 230 场。
  - 档案页确实**列出了 `s30_s1`、`s30_s2`** 两个赛季选项，所以不是我们没去要。
  - `s29_s2` 原始 HTML 里最新会话的时间文案是 **`1 month 26 days ago`**，
    其余依次 1m27d / 2m1d / 2m5d…（与今天 09-14 反推 ≈ 07-19，吻合）。
  - 隐藏图表载荷 `mmChartv3JsonPayload` 共 233 点，**最新时间 `2026-07-18T19:21:43Z`**
    （rankScore 12389），最旧 `2026-06-24T07:50:21Z`。
  - 由此确认：我们的时间戳配对逻辑**正确**，没有把近期对局错标成 07-19。
- **对比 tracker.gg**：它显示「1 day ago / Octane / Level 1,147」等更近的会话，
  因为它走的是 Respawn 官方 API（TRN 自建采集），**不是 ALS**。
  两者差距 ≈ 2 个月，属数据源差异。
- **可选项**（未实施，待用户决定）：
  1. 接受现状 —— ALS 免费、无需 Key，但该玩家的对局记录停在 07-19；
  2. 配 `TRN_API_KEY` 走 `--provider trn` 拿更新的对局历史
     （注意 10 次/分钟限流，我们已加 `TRN_LIMITER`）；
  3. 只用 ALS 但把「数据最后更新于」显式标出来，避免误以为页面坏了。
- **坑与教训**：
  - **「页面没变」不必然等于「代码没生效」**。这次我先怀疑时间戳解析，
    但用原始 HTML 复核后确认解析正确、是上游没数据。
    排查这类问题要**先看上游原始响应**，再回头看自己的解析层。
  - 上一轮我为了「确认字段」打开了 tracker.gg；对比时又发现它比 ALS 数据新。
    这提醒一点：**不同数据源的新鲜度不同**，跨源比较容易得出错误结论
    （以为自己的抓取坏了）。
  - 需求理解偏差的代价是一次完整功能的编写 + 撤销。**需求含糊时先问一句**，
    比事后返工便宜得多。

### 新增「本地存档」页面：把本地采集数据搬上网页
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - 新增 `SHOW_ARCHIVE` 开关与 `#pageArchive` 独立页面 + 「本地存档」页签。
  - **用上了两个一直存在、却从未被前端调用的接口**：
    - `GET /api/history` —— 读 `data/history.jsonl`（每次采集一行摘要）+ 快照目录列表
    - `POST /api/collect` —— 从网页触发一次完整采集（真实写盘：快照 / history / 笔记 / 报告）
    （`README.md` 第 47 行早已记录这两个接口，但 `web/index.html` 从未调用过。）
  - 页面内容：
    - 汇总行：采集次数 / 时间跨度 / RP 总变化 / 磁盘快照数
    - 采集记录表：每行一次采集 —— 时间 · 段位 + RP · 等级（带与上次的变化）·
      传奇数 · 生涯击杀 · 与上次的 RP 变化（首次显示「基线」）
    - 磁盘快照标签列表（`title` 保留原始目录名）
    - 「立即采集」按钮 + 提示文案
  - RP 无变化时额外给一行说明（「采集到的数据本身没变，不是抓取失败」），
    避免把「数据没变」误读成「采集失败」。
  - 新增 `loadArchive()` / `renderArchive()` / `collectBar()` / `bindCollect()`；
    `showPage()` 扩展为三页；`goto('archive')` 每次进入都重读本地数据。
- **原因**：用户要求「更新本地数据到网页」。经核对，服务端早已提供本地历史接口，
  但前端从未使用 —— 本地采集的存档（7 次记录 + 6 个快照）在网页上完全看不到。
- **验证**：
  - 页面渲染：`ARCHIVE 本地存档 8 次采集`；汇总
    `采集次数 8 / 时间跨度 09-10 00:26 → 09-14 03:36 / RP 总变化 0 / 磁盘快照 7 个`；
    表格 8 行，首行标「基线」，传奇数在最后一次由 28 → 27（对应之前修 Global 过滤）。
  - **从网页触发采集成功**：点击「立即采集」后 `采集次数 7 → 8`、`磁盘快照 6 → 7`、
    新增行 `09-14 03:36`、新增快照 `2026-09-13T19-36-15-034Z`。
  - **磁盘核对**（证明是真写盘，不是只改 UI）：快照 6 → **7** 个、`history.jsonl` 7 → **8** 行、
    最新快照含 `als.html.gz` + `meta.json` + `parsed.json`、`history.jsonl` 末条
    `2026-09-13T19:36:18.166Z | RP 12368 | Lv 147 | 传奇数 27`；无空目录。
  - 布局：1500px / 1100px 下 6 列 `112px 362px 132px 96px 132px 68px`，
    横向溢出 0；页签 4 个且 0 禁用。
- **坑与教训**：
  - **`snapshots` 目录名是 ISO 时间戳把 `:` 和 `.` 换成 `-` 的结果**
    （`2026-09-13T19-32-26-039Z`）。第一版我用正则粗暴替换，得到
    `2026-09-13 19-32:26:039` 这种半吊子格式；改为**解析出日期时间分量再重组**，
    显示为 `09-13 19:32`，原始目录名放 `title`。
  - **响应式隐藏列不能用 `:nth-of-type`**：`.arow` 的子元素全是 `div`，
    `:nth-of-type(n)` 按 **div 序号**计（1=.at 2=.ar 3~5=.ac 6=.ad），
    写 `.ac:nth-of-type(n+3)` 实际隐掉的是前两个 `.ac`，与意图相反。
    改用明确的 `:nth-child(4)` / `:nth-child(5)` 并加注释说明原因。
  - `/api/collect` **不带 `--matches`**，所以网页采集出的快照只有 3 个文件
    （无 `matches.json`）—— 与命令行 `--matches 10` 的结果不同。
    若希望网页采集也带对局数据，需要给 `server.mjs` 的 `collect()` 传 `--matches`。

### 更新采集数据（第 7 次采集）
- **类型**：数据
- **文件**：`data/snapshots/2026-09-13T19-32-26-039Z/`（新增）、`data/history.jsonl`、
  `Apex 战绩.md`、`apex-report.html`（自动重新生成）
- **改动**：
  - 执行 `node track.mjs --name bluekinger --matches 10`，采集最新战绩。
  - 新快照含 4 个文件：`als.html.gz`(15KB) / `matches.json` / `meta.json` / `parsed.json`(21KB)。
  - `history.jsonl` 6 → **7** 行；笔记与报告重新生成（时间戳 03:32）。
  - 清理了一个**空快照目录** `2026-09-13T19-29-47-790Z`（失败尝试留下的，见「坑与教训」）。
- **原因**：用户要求「更新数据」。
- **验证**：
  - 终端摘要：`Diamond IV / 12,368 RP / Top 8.65% / Lv 147 / Prestige 2`，
    追踪器 `BR Kills, BR Wins, BR Damage`，生涯 `Career Kills 9,164 (Top 27.25%)`、
    `Career Revives 825`、`Career Wins 335`，传奇 27 个。
  - 最近 10 场：净 RP **+571**、140 分钟，逐场 RP 变化与本地渲染一致。
  - `history.jsonl` 末条 `2026-09-13T19:32:32.415Z | RP 12368 | diamond4 | Lv 147 | 传奇数 27`。
  - 笔记首行自我声明 `最后更新：2026-09-14 03:32 · 共 7 次采集`。
  - 快照逐个清点：6 个目录**均有 3~4 个文件**，无空目录残留。
  - 本地服务仍正常（`GET / → 200`，监听 127.0.0.1:8787）。
  - **数据本身无新增**：与上次对比「无变化」，最新对局仍是 07-19 —— 说明这段时间没打新对局，
    不是抓取失败（下面的健康检查可佐证）。
- **坑与教训**：
  - **网络是间歇性不稳定的**，不是被 ALS 封禁。诊断记录：
    首次 `node track.mjs` 报 `❌ fetch failed`；逐层排查得
    `UND_ERR_CONNECT_TIMEOUT`（连接超时，curl 返回 000，卡满 20s），
    但同一时刻对比多个站点发现——`apexlegendsstatus.com` **200 / 1.4s 可达**，
    而 `api.github.com`、`api.mozambiquehe.re` 超时、`www.baidu.com` 竟要 10s。
    结论：本地出口网络抖动，**与目标站点无关**。重试即成功。
    → 遇到 `fetch failed` 先做**多站点对比**再判断，不要直接归因于被封/限流。
  - **`fetch failed` 会留下空快照目录**：`main()` 里 `mkdir snapDir` 发生在抓取之前，
    所以任何抓取失败都会留下一个空目录（本次出现了 2 个：19-28-55、19-29-47）。
    目前只能手动 `rmdir` 清理。**可改进方向**：把 `mkdir` 推迟到首次写文件前，
    或失败时在 `catch` 里回收目录。本次未改代码（用户只要求更新数据）。
  - 首次采集用 `| tail -40` 观察输出，导致**输出被全缓冲**、工具误判为空闲而移入后台，
    进而被中断（`^C`）。**跑这类「会写文件」的命令不要接 `tail`/`head` 管道**，
    否则既看不到进度、又容易被当成空闲中断，留下半成品。

### 给 TRN 加限流，并确认默认路径不访问 tracker.gg
- **类型**：修复
- **文件**：`track.mjs`、`README.md`
- **改动**：
  - 新增通用 **滑动窗口限流器** `makeLimiter(max, windowMs = 60_000)`：
    保证任意 60 秒内不超过 `max` 次请求（而非固定间隔），
    正常突发调用不被拖慢、连续反复运行也不会越过上限。
  - 新增 `TRN_LIMITER = makeLimiter(8)` —— **tracker.gg 官方限制 10 次/分钟，
    这里只用 8 次留安全余量**。
  - `fetchTrn()` 的 `get()` 在发请求前 `await TRN_LIMITER()`，并补了 `429` 的
    专门报错文案（原先把 429 混在通用错误里，看不出是限流）。
  - `fetchMatches` 的赛季循环加 600ms 礼貌间隔（仅第 2 个赛季起），
    避免 `--matches all` 把 ALS 打成一串突发请求。
  - `README.md` 新增「速率限制：10 次/分钟」小节：说明默认不碰 tracker.gg、
    限流器所在位置与配额，并明确**不要为看数据去刷 tracker.gg 页面**。
- **原因**：用户提醒「不要频繁拉取 tracker.gg，它限制一分钟只能 10 次」。
- **验证**：
  - **限流器语义**（离线单测，用 3s 窗口 / 2 次上限等价缩放）：
    前 2 次立即放行、第 3~4 次等到 t+3051ms、第 5 次等到 t+6103ms；
    **任意 3s 窗口内最大放行数 = 2（未越上限）**；总耗时 15.3s，证明是等待而非直接放行。
  - **默认路径无回归**：`--matches 10` → 10 场，耗时 6574ms。
  - **静态确认**：`provider` 默认值仍是 `'als'`；`fetchTrn` 只在
    `args.provider === 'trn'`（第 983 行）时被调用 —— 默认流程不会请求 tracker.gg。
  - `node --check track.mjs` 通过。
- **坑与教训**：
  - **「限流」不等于「固定间隔」**：如果简单地每次 sleep 6s，一次运行 3 个请求就要
    18s，体验很差；而语义上真正的约束是「60 秒内 ≤10 次」。
    滑动窗口既允许突发、又保证不超限，才是对得上配额的实现。
  - 用户提醒前，`fetchTrn` **完全没有限流**，只有失败重试的退避。
    也就是说配了 Key 且反复运行确实会撞上限流 —— 这类「只在配置了可选 Key 时才暴露」
    的风险容易被漏掉，值得专门补一层。
  - 我此前为了看 tracker.gg 的展示字段，用浏览器打开了它的页面（2 次加载 + 1 次刷新）。
    **这本身就在消耗对方的配额**，而且页面被 Cloudflare 防护、数据接口还是坏的，
    白耗额度且没拿到有效信息。**以后不要用浏览器访问它**，需要什么字段就问用户或查文档。

### 对局记录平铺全部字段 + 后端保留全量拉取能力
- **类型**：功能
- **文件**：`web/index.html`、`track.mjs`、`server.mjs`
- **改动**：
  - **前端**（`web/index.html`）
    - 展示场次 `MATCH_LIMIT` 由 20 改为 **10**；新增 `MATCH_LIMIT_ALL = 'all'` 常量，
      标注为「将来入库后可启用的全量入参」。
    - 新增 `STAT_CN`（通用统计键 → 中文）与 `statCN()`。**只翻译含义明确的通用项**
      （Kills/Damage/Revives/Headshots/Wins/Career Kills）；传奇专属追踪器是游戏内的
      技能/道具名，无统一权威译名，**保留英文原文**，避免显示错误的中文。
    - 新增 `matchExtra(m)`：对局行的**第二行**，平铺该场全部数据 ——
      等级、XP、其余追踪器数值，末尾列出**该场追踪器组合的原始键名**。
      Kills/Damage 不重复列值（已在第一行），但会出现在组合名列表里，
      这样「为什么第一行是击杀/伤害」一眼能对上。
    - `.mrow` 追加 `${matchExtra(m)}`；新增 `.extra` 系列样式：
      `grid-column:2/-1` 占第 2~4 列、虚线分隔、自动换行，不参与第一行列宽分配。
  - **后端**（`track.mjs`）
    - `--matches` 支持 **`all`**：`/^all$/i.test(v) ? Infinity : …`。
    - `fetchMatches` 的 `maxSplits` 默认值改为**自适应**：
      `maxSplits ?? (Number.isFinite(limit) ? 4 : Infinity)` ——
      有限场次仍只翻 4 个赛季（控请求数），`Infinity` 时翻完所有赛季。
      函数注释里标明了这是**将来数据库采集的接入点**。
  - **后端**（`server.mjs`）
    - `query()` 支持透传 `matches: 'all'`；数字入参的上限由 200 放宽到 2000，
      并注明「这只是单次查询上限，不是对局总数上限」。
- **原因**：用户指出 tracker.gg 的对局页展示了更多字段，要求把我们的对局记录也补全、
  全部平铺；同时明确「前端只加载最近 10 场，但后端要能拉取所有，数据库还没建，先保留功能」。
- **验证**：
  - **后端全量**：CLI `node track.mjs --json --no-save --name bluekinger --matches all`
    → **2484 场 / 6 个赛季**（s29_s2 690、s29_s1 738、s28_s2 315、s28_s1 735、s26_s2 3、s24_s2 3），
    时间范围 2025-03-29 → 2026-07-18，耗时 **59s**。
  - **服务端全量**：`POST /api/query {matches:'all'}` → `ok:true`，**2484 场 / 6 赛季**（55.7s）；
    `{matches:10}` → `ok:true`，10 场。
  - **前端渲染**：对局卡 `MATCHES 对局记录 10 场`，10 行、10 个 `.extra` 第二行；
    样例 `瓦尔基里 | BR · Storm Point · 16m 9s | 击杀 0 伤害 761 | +64 | 12,389 RP` +
    第二行 `等级 1,110 · XP 3,240 · 生涯击杀 0 | 追踪器 Kills · Career Kills · Damage`。
    `rpInsideViewport = true`，横向溢出 0。
  - **构造用例**（直接调 `matchExtra` 覆盖真实数据里没出现的分支）：
    传奇专属键 `Focus of Attention: Enemies Hit` / `Gift From The Rift: Items Seized` /
    `Bamboozles` / `Into the void: Time` 均**保留英文**且正常显示数值；
    `stats:{}` → 只显示等级/XP、**不生成空的追踪器分组**；缺 level/xp → 只显示追踪器分组。
  - **无回归**：走势图 10 点、底部 `净 RP +571 / 场均 +57 / 最好 +306 / 最差 -86`；
    常用传奇 3 行（瓦尔基里/兰伯特/探路者）；页签 3 个且 0 禁用；头像与段位徽章 10/10 加载。
- **坑与教训**：
  - **写帮助文本时踩了模板字符串的坑**：`track.mjs` 的 `--help` 输出整体是一个模板字符串，
    我在里面写了 `` `all` ``（反引号），直接闭合了模板字符串 → `SyntaxError: missing ) after argument list`。
    同理，模板字符串内不能用反引号做强调，改用「」或去掉。
  - 一开始把「追踪器」分组写成 `追踪器 N 项` + 其余数值，结果出现**数量与可见值不一致**
    （显示「3 项」却只列 1 个值，因为 Kills/Damage 被排除但计入了数量）。
    改为直接列出**组合的原始键名**，数量歧义自然消失，还顺带解释了第一行的来源。
  - 后端「能拉全量」与「前端只显示 10 条」是两件事：前者是**能力**，后者是**展示策略**。
    把上限写死在展示层会顺手废掉采集能力，所以两者用独立常量/参数表达。
  - 全量拉取 2484 场要 55~59s、几十个请求，**目前只在内存里、不落盘**；
    真正用起来必须配数据库（见 `fetchMatches` 注释里标的接入点）。

### 英雄名称改为中文（国服简中译名）
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - 新增 `LEGEND_CN` 映射表（28 个传奇，含 4 个较新角色）与两个助手：
    `legendCN(name)`（英文名 → 中文名，表里没有的原样返回）和 `legendInitial(name)`
    （头像字母兜底改用中文首字）。
  - 以下显示位置全部改用中文名：Legends 卡片名、传奇击杀排行标签、
    常用传奇行名与「主力传奇」汇总、对局行名与「最常使用」汇总、各头像的字母兜底。
  - 头像/卡片元素补 `title="英文原名"`，便于与英文资料对照。
  - **并列排序键统一为中文名**：原先初次渲染按英文名排、DOM 重排按 `data-n` 排，
    两处口径不同会导致往返排序后顺序漂移；现两处都用 `legendCN()`。
- **原因**：用户要求「调整英雄名称为中文」。
- **译名来源与核对**：以 **biligame 的 Apex 维基**（对应国服简中）为主，
  逐条比对维基百科中文版的赛季表（新传奇条目）。
  后者能佐证 `命脉`(Lifeline，第 23 赛季重做条目写作「传奇命脉」)、
  `万蒂奇`、`卡特莉丝`、`弹道`、`导管`、`变幻` 等。
  **两个来源冲突项已在代码注释中标注**：
  - `Sparrow`：维基百科作「雀影」，biligame 作「琉雀」→ 取 **琉雀**。
  - `Axle`：第 29 赛季新传奇，暂用维基百科的 **艾瑟儿**。
- **验证**：
  - **覆盖率**：拉取真实 profile 的 27 个传奇逐一查表，`missing = []`（27/27 命中）；
    表内 `Axle` 本次数据未出现（它只出现在对局里），属预期。
  - 页面文本扫描：`leftoverEnglishNames = []` —— 结果区内不再残留任何英文传奇名。
  - 实测渲染：常用传奇 `瓦尔基里 / 变幻 / 兰伯特 / 探路者`；
    对局行 `瓦尔基里 / 探路者…`；汇总 `最常使用 瓦尔基里 8 场`、`主力传奇 瓦尔基里 40%`。
  - 临时打开 `SHOW_LEGENDS` / `SHOW_RANK` 验证（**测毕已还原为 false**）：
    27 张卡片与排行的 12 个标签均为中文（`1. 瓦尔基里` … `12. 动力小子`）；
    切「按名称」排序结果为 `艾许 / 班加罗尔 / 暴雷 / 变幻 / 弹道 / 导管 / 地平线 / 动力小子`（按拼音有序）；
    **往返排序稳定** `roundTripStable = true`。
  - **图片 slug 仍为英文**：实测 `Valkyrie.webp / Pathfinder.webp / Bangalore.webp / Wraith.webp`，
    且全部 `naturalWidth > 0`（证明中文名没有污染 `legendImg()`）。
  - 横向溢出 0；页签 `Overview / 常用传奇 / 对局记录`。
- **坑与教训**：
  - **数据键是英文，显示才用中文**：`legendPrimaryStats` / `stats` / `matches` 的 legend 键
    始终是英文原名，中文名只能用于渲染文本。绝不可以用中文名去查数据或拼图片 slug，
    否则既查不到数据、又会 404。
  - **改了显示名就必须同步改所有排序/比较口径**。本次就踩到了：把 `data-n` 换成中文后，
    初次渲染仍按英文名做并列排序键，导致「按击杀 → 按名称 → 按击杀」往返后顺序漂移
    （`sortStable = false`）。统一为 `legendCN()` 后才稳定。
  - 两个权威来源对同一传奇的译名不一致（`Sparrow`），这类冲突不能默默选一个了事，
    应在代码里注明另一说法与取材依据，方便日后核对与修改。

### 页面默认按桌面版渲染（不再随容器变窄降级）
- **类型**：视觉 / 修复
- **文件**：`web/index.html`
- **改动**：
  - `<html>` 加上 `class="desk"` 作为**总开关**。
  - `<meta name="viewport">` 由 `width=device-width,initial-scale=1` 改为 `width=1280`，
    让手机等真窄设备也按桌面版缩放着显示。
  - 新增 `html.desk .wrap{min-width:1080px}`：内容区不小于桌面设计宽度，
    容器更窄时**横向滚动**而不是压缩卡片。
  - 新增 `html.desk body{overflow-x:visible}`：放开原本 `body{overflow-x:hidden}` 的裁切，
    让视口接管横向滚动。
  - 全部 **6 处窄屏规则**（`600` hero 读数框 / `720` 搜索栏 / `820` 骨架屏 /
    `820` 两栏 / `720` 常用传奇行 / `720` 对局行）统一加上 `html:not(.desk)` 守卫。
  - `#trendCard` 的两条 **container query 保持不变** —— 它们只是按卡片实际宽度补偿
    SVG 缩放导致的字号过小，不属于「移动版降级」。
- **原因**：用户要求「默认显示页面为桌面版」。此前页面是响应式的，容器窄于 820px 就切成
  单栏移动版，用户在窄窗口里看到的始终是移动版。
- **验证**：同一 `page.evaluate` 内测得（避免视口漂移干扰）
  - **380px / 700px**：`matchMedia('(max-width:820px)')` 与 `(max-width:720px)` **均为 true**
    （媒体查询确实命中了），但布局仍是桌面版 —— `.cols` = `296px 730px`、
    `.row` = 4 列、`.pick-row` = `42px 1fr 60px 116px`、`.mrow` = `38px 640px 152px 104px`、
    `.readout` = `column`、按钮宽 118px（非满宽）、`.wrap` 宽 1080、可横向滚动。
  - **1440px**：同款桌面布局，且无需横向滚动。
  - 退路可用：移除 `desk` 类后同一 380px 下立刻变回单栏
    （`.cols` `340px`、`.readout` `row`、按钮满宽 340px、`.wrap` 380），
    重新加回即恢复桌面版。
  - `meta[name=viewport].content === 'width=1280'`。
  - `window.scrollTo(0,400)` 生效（`scrollY = 400`），`document.scrollingElement === html`
    —— 确认 body 没有被变成滚动容器。
- **坑与教训**：
  - **不能给 body 设 `overflow-x:auto`**：一旦某轴非 `visible`，另一轴会被计算成 `auto`，
    body 就变成滚动容器，`window.scrollTo` / `scrollIntoView` 会失效
    （本项目页签跳转依赖它）。必须用 `visible` 放开裁切，让视口负责横向滚动。
  - `min-width` 不能省：窄容器下两栏会被压到右栏仅 350px，而对局行本身有
    `152px + 104px` 两列定宽，留给传奇名只剩不到 30px，必然崩版。
    有 `min-width` 才能保证「要么完整桌面版，要么横向滚动」。
  - 用「媒体查询命中 + 计算样式仍是桌面」这一对矛盾来验证守卫，比单看样式更有说服力 ——
    能直接证明是 `html:not(.desk)` 在起作用，而不是媒体查询压根没触发。
  - 保留了「去掉 `desk` 类即恢复响应式」的退路，避免把已知可用的移动端适配彻底删掉。

### 修复灰页签：页签改为按注册表动态生成
- **类型**：修复
- **文件**：`web/index.html`
- **改动**：
  - HTML 里写死的 5 个页签按钮改为空容器 `<nav class="tabs" id="tabs"></nav>`，
    全部由 JS 生成。
  - 新增 `TABS` 注册表（`{id, target, label, show, title}`），
    页签**只渲染 `show()` 为真的项** —— 关掉的区块不再留下按钮。
  - 新增 `tabReady(t)`：flag 开着**且目标确实存在**。
    `picks` / `matchCard` 是固定页面，看 flag 即可；`legendCard` / `rankCard`
    依赖数据（某玩家没有带击杀的传奇就根本不会生成），必须查 DOM。
  - 新增 `buildTabs()`：重建按钮、重新绑定点击、恢复高亮。
    高亮项若已消失（数据变化导致区块被移除）则**回落到第一项**。
  - 新增 `setActive(target)` 统一管理高亮；`goto()` 开头调用它。
  - `loadMatches()` 在「点了页签但还没查询过」时给出「等待查询」提示，
    不再静默无反应；同时移除了内部的页签高亮操作（交给 `goto` 的 `setActive`）。
  - `show()` 为假时不再用 `disabled` + `opacity:.38` + `cursor:not-allowed` 的旧做法。
  - 首帧（还没有数据时）先调一次 `buildTabs()`。
- **原因**：用户确认修复「所有灰页签」。此前 `Legends` / `排行` 因对应区块被开关关闭而
  渲染成**灰色的禁用按钮**，点不动，看起来像功能坏了。
- **验证**：
  - 当前配置（`SHOW_PICKS` / `SHOW_MATCHES` 为真，`SHOW_LEGENDS` / `SHOW_RANK` 为假）
    页签为 `Overview / 常用传奇 / 对局记录`。
  - 1440 / 900 / 380 三档实测：`disabledCount = 0`、`fadedCount = 0`、
    `cursorNotAllowed = 0`、`allHaveTargets = true`、横向溢出 0。
  - 临时把 `SHOW_LEGENDS` / `SHOW_RANK` 置真后重新验证（测毕已还原为假）：
    5 个页签全部出现且无禁用态；实际 DOM 中 `#legendCard` 有 27 张卡、`#rankCard` 有 12 项。
    打桩 `scrollIntoView` 记录点击落点依次为 `legendCard` / `rankCard` / `matchCard` / `""`（回顶部 body），正确。
  - 边界：移除 `#rankCard` 后调 `buildTabs()`，「排行」页签随之收起；
    在高亮「Legends」时移除 `#legendCard` 再重建，高亮**回落为 Overview**。
- **坑与教训**：
  - 「点不动」比「看不见」更糟：宁可**不渲染**，也不要渲染成禁用态。
    禁用按钮会让人以为功能坏了，而隐藏按钮只是功能尚未开放。
  - 页签的可用性判断不能只看开关：`legendCard` / `rankCard` 是**数据依赖**的，
    开关开着也可能没生成 → 必须结合实际 DOM，否则又会造出「点了没反应」的按钮。
  - 临时改开关做验证后必须还原，并在日志里写明「测毕已还原」，避免状态含糊。

### 建立开发日志
- **类型**：文档
- **文件**：`开发日志.md`（新增）
- **改动**：
  - 新增本文件，并规定「每次修改项目文件，都必须在同一次操作里往本文件追加一条记录」。
  - 定义条目模板与规则：最新条目在最上面、日期与 `git log` 对齐、
    「验证」须写可复核证据、「坑与教训」重点记录走不通的路与原因。
  - 回溯补齐 2026-09-13 ~ 2026-09-14 的既有改动（对局记录、RP 走势图、常用传奇、
    战术 HUD 改版、显示开关组等）。日期精确到天，未保留分钟级时间。
- **原因**：用户要求「生成一个开发日志 md 文档，在修改项目时记录，每次修改都同时添加到文档里」。
- **验证**：文件创建成功；条目结构自洽（每个 `###` 条目均含类型 / 文件 / 改动 / 原因 / 验证 / 坑）。
- **坑与教训**：写在项目里的「约定」只能约束读它的人；想让后续的助手也持续遵守，
  必须把同一条规则同时写进**仓库记忆**，否则新会话不会主动维护本文件。

### 启用「对局记录」并适配 HUD 排版
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - `SHOW_MATCHES` 由 `false` 改为 `true`，对局记录卡片正式出现在第一页末尾。
  - 对局行的「击杀 / 伤害」列由 `1fr` 改为**定宽 152px**。原写法在 1040px 宽的卡片里
    被拉到 363px，而内容只有 ~120px，中间空出一大片。
  - 「击杀 / 伤害」改为**右对齐**（`justify-content:flex-end` + 每格定宽 66px +
    `b{margin-left:auto}`），使两列数值贴到右侧、与 RP 列拼成一个紧凑数据块；
    配合 `font-variant-numeric:tabular-nums`，跨行数字按右边缘对齐。
  - 无追踪数据的场次补占位符 `追踪数据 —` 并加 `.ks.na{opacity:.36}`。
  - ≤720px 隐藏击杀列（`38px minmax(0,1fr) 92px`）；逐行微调 RP 里的段位徽章为 15px。
- **原因**：用户要求「添加对局记录功能」。此前该模块已实现但因开关关闭而不显示；
  打开后暴露出三处宽屏排版问题（列被拉伸、数字不对齐、空数据行视觉断裂）。
- **验证**：
  - 卡片渲染 `MATCHES 对局记录 20 场`；汇总 `20 场 / 净 RP +993 / 4 小时 33 分 / Valkyrie 8 场`。
  - 分组头 `S29 · 第 2 段 · 07-19 03:21 · 20 场 · 净 RP +993`。
  - 三档断点实测列宽：1440px → `38px 640px 152px 104px`；900px → `38px 460px 152px 104px`；
    380px → `38px 126px 92px`（击杀列 `display:none`）。
  - 跨行数字右边缘唯一：击杀全部 704px、伤害与占位符全部 784px。
  - 横向溢出三档均为 0；20 场中有 6 场无追踪数据，占位符正常显示。
- **坑与教训**：
  - 该浏览器页被标记为 *not visible* 时 `requestAnimationFrame` 不运行，
    `behavior:'smooth'` 的滚动**永远走不完**，`window.scrollY` 恒为 0。
    一度误判「点页签不滚动」，给 `Element.prototype.scrollIntoView` 打桩后才确认调用链正确。
  - `setViewportSize()` **不会保持**，会被环境立刻重置回窗口实际宽度（约 954px）。
    导致「按 954 布局截图、按 380 测量」，一度误判「移动端丢掉 RP 列」。
    对策：把**设置视口、滚动、测量、命中测试全部压进同一次 `page.evaluate`**。
  - 判定元素是否真的渲染出来，最可靠的是**原子命中测试**：同一次求值内先读
    `getBoundingClientRect()` 再 `elementFromPoint()` / `elementsFromPoint()`。

### 视觉重做：战术 HUD（军工数据台）
- **类型**：视觉
- **文件**：`web/index.html`
- **改动**：
  - 用 `frontend-design` 技能定方向，从射击游戏**游戏内 HUD** 取材，
    目标从「深色卡片网页」改为「通电的设备外壳」。
  - 重建设计令牌：表面四层（`--void/--deck/--plate/--plate-2/--plate-3`）、
    分隔线两档（`--hair/--hair-2`）、文字三档（`--ink/--ink-2/--ink-3`）。
  - 三个功能色**职责不重叠**：`--amber #ff9f28`（主强调）、`--steel #5ed0d8`（正向/涨）、
    `--ember #ff5a37`（负向/错误）。
  - **字体是本次设计的个性核心**：正文走系统 UI 栈，但**所有数值与微标签改走等宽栈**
    （`ui-monospace/SF Mono/Menlo`），配合 `letter-spacing:.1em~.22em` + 全大写 +
    `tabular-nums`。零外部字体。
  - 背景不再是纯色块：`body::before` 46px 细网格（径向渐隐）+ `body::after` 扫描线
    + 内联 SVG `feTurbulence` 噪声颗粒。
  - hero 改为「干员档案头」：`.hero-lay-*` 三层（23px 网格 / 琥珀主光 / 缓慢扫过的扫描带）、
    `— OPERATOR DOSSIER` eyebrow、切角方形头像、右上 `LINK STATUS` 状态灯、
    底部斜纹警示带 `.tape`。
  - 形状语言统一：面板直角 + 左上/右下 L 形角标（`::before/::after`）；
    小构件用 `clip-path` 切角（头像 15px、徽标 6px、按钮 9px），全线取消圆角。
  - 面板标题栏统一为 `▮ CODE │ 标题 ──── 说明`，模块代码由 JS 输出：
    Rating / Profile / All-time / Kills / Roster / Trend / Picks / Matches。
  - 数据格 `.stat` 改为左侧琥珀渐变刻度条 + 顶部小帽；走势图配色调为琥珀→钢青（跌则余烬）。
  - 新增卡片错落入场动画（`nth-child` + `animation-delay` 阶梯），并加
    `prefers-reduced-motion` 兜底。
- **原因**：用户选定「战术 HUD（军工数据台）」方向，要求只重做**当前可见部分**，
  且**不使用外部字体**（靠系统栈精心搭配）。原 tracker.gg 风格偏通用。
- **验证**：
  - 原子命中测试 8/8 通过（`#tabTop` `#tabPicks` `#uid` `#name` `#platform` `#go`
    `.eyebrow` `.tabs`）。
  - 关键数值实测走等宽栈：`.ranknow .rp`、`.card>h2 .code`、`.trend-foot .v`。
  - 走势图渐变 `#ff9f28 → #5ed0d8`；`.card::before` 角标 `rgba(255,159,40,.32)`。
  - 弃用区块的 CSS 探针通过：`.stat` padding-left 14px + 琥珀渐变条 + 31px 等宽琥珀数值；
    `.lg` 头像切角生效；`.mrow` 列宽 `38px 354px 295px 104px`、涨色 `rgb(94,208,216)`。
  - 380 / 954 / 1440 三档横向溢出均为 0。
- **坑与教训**：
  - `.tape` 高度 ≤3px 时 135° 斜纹会退化成「虚线」，看不出警示带 → 改 4px + 45° 斜纹。
  - `.card>h2 .sub` 已有 `margin-left:auto`，若标题栏再放 `.tools`（也有 `margin-left:auto`），
    两个 auto 会把标题栏扯开 → 加 `.sub~.tools{margin-left:12px}`。
  - 长块 `oldString` 匹配多次失败，但逐行 `includes()` 校验却全部通过。
    最终靠**拆成多条小改动**逐个应用才成功；说明编辑工具对超长多行块较脆弱，
    长段落重写应主动拆分。
  - 保留旧变量名兼容别名（`--gold/--accent/--ok/--err/--panel/--line/--text/--muted/--dim`），
    避免遗漏引用变成无效值。

### 「常用传奇」独立成页
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - 新增**页面模型**：`#result` 下改为若干 `.page` 容器 —— 第一页 `#pageOverview`，
    新增 `#pagePicks`；用 `showPage('overview'|'picks')` 切换 `hidden`。
  - 显式声明 `.page[hidden]{display:none}`，否则可能被 `display` 覆盖而失效。
  - 页签扩为 5 个（Overview / 常用传奇 / Legends / 排行 / 对局记录），
    顺序与 `TABS` 数组严格一致。
  - 「常用传奇」从**对局数据**聚合（`pickStats()`）：出场数 / 净 RP / 击杀 / 伤害 / 胜场；
    排序 = 出场数 ↓ → 净 RP ↓ → 名称（保证稳定）。**零额外请求**，只读 `matchCache`。
  - `ensureMatchCard()` 改为挂到 `#pageOverview`（而非 `#result`），否则卡片会跑到页面外层。
  - 窄屏 ≤720px 隐藏「场次」列与「主力」徽章，改用**金色序号 + 金色底**标识主力。
- **原因**：用户要求「只修改前端、不涉及后端接口，需要数据的先调用之前的缓存；
  对常用传奇添加到单独的页面，不再第一个页面显示」。
- **验证**：
  - 第一页文本中**不含**「常用传奇 / 主力」；`#pagePicks` 初始 `hidden=true`。
  - 点页签后 `#pageOverview` 隐藏、`#pagePicks` 显示、高亮切换正确，渲染 4 行。
  - 数据与后端独立统计逐项吻合：Valkyrie 8 场 40% +714（击杀 9 / 伤害 4,035）、
    Alter 5 场 25% +472、Rampart 4 场 20% −166、Pathfinder 3 场 15% −27。
  - 打开页面仍只有 2 次 `/api/query`；切回页面仅 27ms（命中缓存）。
  - 在 picks 页改玩家查询会自动回到第一页，不残留旧页。
- **坑与教训**：
  - 视口设 390 时实际 `innerWidth` 只有 341（滚动条等），「主力」徽章把传奇名挤到
    ellipsis 截断（名字列只剩 98px）→ 隐藏徽章 + 收窄内边距后才完整显示。
  - 页签从 4 个加到 5 个后，窄屏 flex 会把按钮压扁、中文页签**竖排成四行**。
    必须 `.tabs button{flex:none;white-space:nowrap}`，配合 `overflow-x:auto` 横滚；
    再加右端 `mask-image` 渐隐作为可滚动的视觉提示。

### 新增「最近表现」RP 走势图
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - 新增右栏「最近表现」卡片，**手写 SVG** 折线图（零外部依赖，不引图表库）。
  - 数据源为对局数组的 `rp` + `timestamp`，**必须按时间正序重排**后绘制。
  - Y 轴值域上下各留 16% padding；网格线与刻度标签用**真实值**（非加过 padding 的边界）。
  - 线用水平渐变（涨 = 绿→蓝，HUD 重做后改为琥珀→钢青）；下方叠同色渐隐面积。
  - 高亮点只画首尾与区间极值，其余点用透明 `<circle>` + `<title>` 给原生 tooltip。
  - 底部指标行：净 RP / 场均 / 最好一场 / 最差一场，涨绿跌红。
  - 新增 `SHOW_TREND` 开关，并与 `SHOW_MATCHES` 共用**同一次请求**（`NEED_MATCHES()`）。
- **原因**：用户选择「右栏填起来」的方案 1 —— 用已有对局数据画 RP 走势。
- **验证**：
  - 折线 20 个数据点，走势与 `rpChange` 累加吻合（净 +993）。
  - 曲线渲染正常（非空白），底部四项数值正确。
  - 打开页面共 **2 次** `/api/query`（主查询 + 对局数据），未因多一张图而增加请求。
- **坑与教训**：
  - **ALS 的 `rp` 是「该场打完后」的分数**，而 `matches` 数组是「新 → 旧」；
    不重排会把整条走势画反。
  - SVG 按 `viewBox` **等比缩放，字号与线宽一起缩**：卡片 350px 宽时缩放仅 0.405，
    13px 字实际只剩 5px。用 **container query**（`#trendCard{container-type:inline-size}`）
    按卡片实际宽度分档放大（≤640 → 17px / 线 3.6；≤420 → 隐藏 Y 轴标签 + X 轴 26px）。
    不能用视口媒体查询 —— 右栏宽度还受左栏影响，看视口判断不准。
  - Y 轴刻度改用 `12,390 → 12.4k` 缩写：viewBox 内左侧留白只有 60 单位，
    放大字号后六位数会顶出边界被 `clip` 裁掉。

### 新增显示开关组，屏蔽左栏以下全部区块
- **类型**：重构
- **文件**：`web/index.html`
- **改动**：
  - `render()` 顶部新增一组开关：`SHOW_ACCOUNT / SHOW_LIFETIME / SHOW_RANK /
    SHOW_LEGENDS / SHOW_MATCHES`，改回 `true` 即可恢复，标记都留在模板里。
  - `main` 为空时 `.cols` 不再输出空的右栏。
  - `loadMatches()` 首行加 `SHOW_MATCHES` 防护（关掉后连自动抓取也跳过，省 2~4 个请求）。
  - `syncTabs()` 中 `matchCard` 的可用性改为 `SHOW_MATCHES && lastQuery`。
- **原因**：用户要求「先屏蔽 Account 以下的所有功能，按我的想法一步步重开」。
  首次只屏蔽了 Account 卡片，被指出理解偏窄后改为屏蔽该位置以下的全部区块。
- **验证**：
  - `#result` 子元素仅剩 `cols`；可见卡片 `Current Rating` / `Account`。
  - 传奇击杀排行 / Legends / Lifetime / 对局均不存在，2.5 秒内也无异步占位冒出。
  - 页签 Overview 可用，其余置灰；横向溢出 0。

### 对局记录改为跟随主查询自动加载
- **类型**：重构
- **文件**：`web/index.html`
- **改动**：
  - 由「点页签才加载」改为 `render()` 末尾 `loadMatches({ auto: true })`。
  - `auto: true` 时**不切页签高亮、不 `scrollIntoView`**。
  - 并发控制由全局布尔 `matchBusy` 改为 `matchInflight`（Set of key）按查询条件去重。
  - 新增 `stale()`：比对 `qKey(lastQuery)` 与请求时的 key，条件不符则丢弃结果。
- **原因**：用户要求「打开页面就自动带上对局，不用点页签」。
- **验证**：
  - 刷新页面零点击：主战绩先出 → 对局卡片占位「加载中…」→ 自动填成 20 场。
  - 页签高亮停在下拉后的 `Overview`，未被对局抢走。
  - 查询后 `scrollY = 0`，页面没有被拽到底部。
  - 竞态测试：请求飞行中换成无效玩家，旧卡片已移除、残留行数 0，无数据串台。
- **坑与教训**：
  - 原来的 `matchBusy` 全局布尔会让「换玩家后的新请求被直接跳过」，页面上对局永不出现
    → 改为 per-key 的 Set 才正确。
  - 平滑滚动与查询结束时 `window.scrollTo({top:0})` 会互相打架，
    所以自动加载路径必须跳过 `scrollIntoView`。

## 2026-09-13

### 补齐「对局记录」前端实现
- **类型**：功能
- **文件**：`web/index.html`
- **改动**：
  - 新增「对局记录」页签与卡片，渲染字段：传奇立绘、名（胜场挂徽章）、
    `模式 · 地图 · 时长`、击杀 / 伤害、`±RP`、段位徽章、累计 RP。
  - 汇总行：场次 / 净 RP / 总时长 / 最常使用传奇。
  - 按 `split + session` 分组（同一时段连打的几局放一起），与 ALS 自身口径一致。
  - 结果按 `uid|name|platform` 缓存（`matchCache`），重复点击命中缓存不发请求。
  - 换玩家查询时先 `lastQuery = null` + 移除 `#matchCard` + 页签复位到 Overview。
- **原因**：用户反馈「战绩显示还是没有」。定位结果为：后端 `--matches` 已可用、
  `server.mjs` 也已透传，但 `web/index.html` 里**完全没有对局相关代码**，
  请求也没带 `matches` 参数 —— 数据拉得到、页面永远看不到。
- **验证**：
  - `node track.mjs --json --no-save --name bluekinger --matches 20` 返回 200 场对局数组。
  - 页面加载 20 场；桌面 1280 / 移动 390 均 0 横向溢出；移动端隐藏击杀列。
  - 对局头像与段位徽章全部 200 且正常绘制。
- **坑与教训**：
  - `matchStats()` **必须精确匹配 `Kills`**，否则 `Career Kills` 会被当成本场击杀。
  - 单场 `stats` 可能是空对象 `{}`，中间列留空在当时属正常（后在 HUD 重做时改为占位符）。
  - 对局里的传奇名与 profile 的 `legends` 名完全一致（含 `Mad Maggie`），
    因此直接复用 `legendImg()` 的 `humanesas` webp 即可 —— 这些图已被 Legends 卡片缓存，
    等于零额外下载。已用 curl 验证 `Axle/Mad-maggie/Wraith/Sparrow.webp` 与
    `ranks/platinum1.png` 均返回 200。
  - ALS 免费接口是 `/core/gameHistory`，**无需 Key**；档案页上「Tier 2+ Patrons 才能下载」
    限制的是**导出/复盘**，不是对局列表本身。

### 诊断：「对局记录」为何不显示
- **类型**：修复
- **文件**：无（只读排查）
- **改动**：无代码改动，产出一份结论。
- **原因**：用户反馈页面上看不到对局记录，且上一次会话的多次重试均无响应。
- **验证**：
  - 实测 `--matches` 抓取链路完全可用（返回 200 场，含真实时间戳与 `rpChange`）。
  - `grep` 确认 `web/index.html` 中无 `matches` / 对局相关逻辑，页签只有 3 个。
  - 由此判定问题在**前端缺失**，而非抓取失败或服务端故障。
- **坑与教训**：上一轮的备忘录曾把该功能标为「已完成」，实际并未落地。
  → 记录状态时要区分「已验证可用」与「已规划」，避免误导后续判断。
